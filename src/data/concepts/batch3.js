// Concepts: Monotonic Queue · Linked List · Fast & Slow Pointers · Recursion · Trees · Binary Search Trees
export const BATCH = {
  "monotonic-queue": {
    id: "monotonic-queue",
    name: "Monotonic Queue",
    summary: "A double-ended queue kept sorted so the window's min/max is always at the front in amortized O(1).",
    overview:
      "## The core idea\n\nA **monotonic queue** is a deque whose contents are kept in sorted order (strictly or non-strictly). For a sliding-window *maximum*, you keep it **decreasing**: before pushing a new value at the back, you pop every back element smaller than it, because those can never again be the maximum while the newcomer lives. The window's max is then always at the **front**.\n\nIt is the missing piece that turns an `O(nk)` brute force over windows into `O(n)`: every index is pushed and popped at most once.\n\n## How to think\n\nAsk three questions. **What am I maximizing/minimizing over a moving range?** **Which old elements are now useless?** A value is useless the moment a newer value is at least as good, since the newer one stays in the window longer. **How does an element leave?** Either it is dominated and popped from the back, or it slides out of the window and is popped from the front. Store **indices**, not values, so you can check expiry against `right - k`.\n\n## Variants\n\n- **Sliding-window maximum/minimum** - decreasing deque for max, increasing for min.\n- **Monotonic-queue optimized DP** - when `dp[i]` needs the best `dp[j]` in a window `[i-k, i-1]`, the deque replaces an inner loop (e.g. Jump Game VI, Constrained Subsequence Sum).\n- **Shortest-subarray-with-sum** - a monotonic deque over **prefix sums** keeps candidate left ends.\n\nEdge insight: store indices and pop the front *only* when it has truly expired (`dq[0] <= right - k`); comparing values for expiry is the classic bug.",
    recognition: [
      "You need the max or min of every contiguous window of fixed size k",
      "A DP transition reads the best dp value over a bounded recent range [i-k, i-1]",
      "Brute force is O(nk) recomputing each window and it TLEs",
      "The phrase 'maximum/minimum in a sliding window' or 'within distance k'",
      "You're scanning prefix sums and need the smallest earlier prefix in a range",
      "A stack would work but elements also expire from the other end as you advance",
    ],
    whenToUse: [
      "Sliding-window maximum/minimum where the window has fixed width k",
      "Optimizing a 1-D DP whose recurrence maxes/mins over a window of previous states",
      "Range-min / range-max queries that move strictly left-to-right (offline, monotone)",
      "Prefix-sum problems needing the best candidate index within a bounded gap",
    ],
    whenNotToUse: [
      "Windows do not move monotonically, or you need arbitrary random-range queries (use a sparse table or segment tree)",
      "You need both max and min simultaneously plus other order statistics (consider two heaps or a multiset)",
      "The aggregate is a sum/product, not an extreme (use a running sum / prefix sum instead)",
      "The window size changes by content rather than a fixed k (that's variable-window two-pointer)",
    ],
    patterns: [
      { name: "Decreasing deque (window max)", note: "pop smaller-or-equal from back before push; front is the max; pop front when index expires" },
      { name: "Increasing deque (window min)", note: "mirror image: pop larger-or-equal from back; front is the min" },
      { name: "Monotonic-queue DP", note: "deque holds candidate j indices ordered by dp[j]; front gives best transition for current i" },
      { name: "Prefix-sum deque", note: "keep prefix sums increasing; pop front to record shortest qualifying subarray, pop back to drop dominated lefts" },
    ],
    complexity: [
      { operation: "Process each element once", time: "O(n)", space: "O(k)", note: "every index pushed and popped at most once" },
      { operation: "Push (amortized)", time: "O(1)", space: "-", note: "back pops are charged to the pushes that caused them" },
      { operation: "Query current extreme", time: "O(1)", space: "-", note: "always the front of the deque" },
    ],
    edgeCases: [
      "k = 1: every element is its own window max",
      "k equal to the array length: a single window over everything",
      "Empty input or k <= 0: return an empty result, do not index the deque",
      "All-equal values: decide < vs <= on the back pop so duplicates are not over-removed",
      "Negative numbers (don't seed the answer with 0; seed with the first element or -inf)",
      "Front expiry must use indices, not values, so duplicate values aren't dropped wrongly",
    ],
    commonMistakes: [
      "Storing values instead of indices, so you can't tell when the front leaves the window",
      "Using < vs <= inconsistently on the back pop, mishandling duplicates",
      "Popping the front by value comparison instead of checking index <= right - k",
      "Forgetting to record the answer only once the window has reached full size k",
      "Treating the deque as a plain queue and scanning it for the max (back to O(nk))",
      "Off-by-one on the window's left edge (right - k + 1 is the first valid start)",
      "Mutating a Python list with pop(0) instead of using collections.deque (O(n) front pop)",
    ],
    template: {
      language: "python",
      code: "from collections import deque\n\ndef max_sliding_window(nums, k):\n    dq = deque()          # stores indices; values are decreasing\n    out = []\n    for right, x in enumerate(nums):\n        # drop smaller values from the back: they can't be the max anymore\n        while dq and nums[dq[-1]] <= x:\n            dq.pop()\n        dq.append(right)\n        # drop the front if it slid out of the window\n        if dq[0] <= right - k:\n            dq.popleft()\n        # window is full once right reaches index k-1\n        if right >= k - 1:\n            out.append(nums[dq[0]])\n    return out",
    },
    observations: [
      "The amortized O(1) comes from a counting argument: each index enters and leaves the deque exactly once",
      "Decreasing deque -> max; increasing deque -> min. Pick by what the front should hold",
      "It is a stack with an extra rule: also evict from the front when items expire by position",
      "For DP optimization, the deque entries are candidate previous states ranked by their dp value",
      "Over prefix sums it answers 'shortest subarray with sum >= K' even with negative numbers, where sliding window alone fails",
    ],
    problems: [
      { title: "Sliding Window Maximum", url: "https://leetcode.com/problems/sliding-window-maximum/", difficulty: "Hard", pattern: "decreasing deque of indices (canonical)" },
      { title: "Jump Game VI", url: "https://leetcode.com/problems/jump-game-vi/", difficulty: "Medium", pattern: "monotonic-queue DP over a window of previous scores" },
      { title: "Constrained Subsequence Sum", url: "https://leetcode.com/problems/constrained-subsequence-sum/", difficulty: "Hard", pattern: "DP[i] += max dp in window k, maintained with a deque" },
      { title: "Shortest Subarray with Sum at Least K", url: "https://leetcode.com/problems/shortest-subarray-with-sum-at-least-k/", difficulty: "Hard", pattern: "monotonic deque over prefix sums (handles negatives)" },
      { title: "Sum of Subarray Minimums", url: "https://leetcode.com/problems/sum-of-subarray-minimums/", difficulty: "Medium", pattern: "monotonic stack/queue counting contribution of each min" },
    ],
    videos: [
      { title: "Sliding Window Maximum - Monotonic Queue - Leetcode 239", url: "https://www.youtube.com/watch?v=DfljaUwZsOk", source: "NeetCode" },
      { title: "Shortest Subarray with Sum at Least K - Leetcode 862 - Python", url: "https://www.youtube.com/watch?v=5AY70aAHZiQ", source: "NeetCode" },
      { title: "Advanced Algorithms 5 - Fast and Slow / Monotonic structures (course)", url: "https://neetcode.io/courses/advanced-algorithms/5", source: "NeetCode.io" },
    ],
    articles: [
      { title: "Sliding Window Maximum (Maximum of all subarrays of size K)", url: "https://www.geeksforgeeks.org/sliding-window-maximum-maximum-of-all-subarrays-of-size-k/", source: "GeeksforGeeks" },
      { title: "Sliding Window Maximum - In-Depth Explanation", url: "https://algo.monster/liteproblems/239", source: "AlgoMonster" },
      { title: "Constrained Subsequence Sum - In-Depth Explanation", url: "https://algo.monster/liteproblems/1425", source: "AlgoMonster" },
    ],
  },

  "linked-list": {
    id: "linked-list",
    name: "Linked List",
    summary: "Nodes chained by pointers - O(1) insert/delete at a known position, but no random access.",
    overview:
      "## The core idea\n\nA **linked list** stores each element in a node that holds a value and a pointer to the next node (and, for a doubly linked list, the previous one). There is no contiguous block, so you reach element *i* by walking *i* hops from the head - **O(n) access** - but once you hold a node you can splice or unlink in **O(1)** without shifting anything.\n\n## How to think\n\nStop thinking 'array' and start thinking 'pointers I rewire'. Almost every problem is one of three moves: **walk** a pointer, **rewire** `next` links, or **build** a new list by appending to a tail. Two tools make the code clean: a **dummy (sentinel) head** so insert/delete at position 0 needs no special case, and **drawing the pointers** before you reassign them - the order of assignments matters because you can orphan the rest of the list.\n\n## Variants\n\n- **Singly vs doubly linked** - doubly costs an extra pointer but allows O(1) delete given only the node.\n- **Reversal** - iterative three-pointer (`prev`, `cur`, `nxt`) or recursive; the backbone of k-group reversal and palindrome checks.\n- **Two-pointer / fast-slow** - find the middle, detect a cycle, or find the n-th from the end in one pass.\n- **Merge / interleave** - thread two lists with a dummy tail (merge sort, merge two sorted lists).\n\nEdge insight: the bugs live at the **head and tail**. Always ask what happens with an empty list, a single node, and the very last `next` (which must end as `None`, or you create a cycle).",
    recognition: [
      "Input is given as a chain of nodes with .next (and maybe .prev) rather than an array",
      "You must insert or delete in O(1) given a node, without shifting elements",
      "The problem forbids using extra arrays / asks for O(1) space rewiring",
      "You need to reverse, reorder, merge, or split a list in place",
      "Cycle detection, finding a middle, or n-th-from-end is requested",
      "Order matters and the structure grows/shrinks at the ends frequently",
    ],
    whenToUse: [
      "Frequent insertion/deletion at the front, back, or a known node",
      "Implementing stacks, queues, deques, adjacency lists, or LRU caches",
      "Merging or splicing sequences without copying the underlying data",
      "When you genuinely cannot afford the O(n) shift of an array insert/delete",
    ],
    whenNotToUse: [
      "You need O(1) random access by index (use an array/list)",
      "You want cache-friendly tight loops; pointer chasing thrashes the cache",
      "Memory is tight - each node carries pointer overhead beyond the payload",
      "Frequent binary search or sorting by value (arrays are far better)",
    ],
    patterns: [
      { name: "Dummy head", note: "prepend a sentinel so insert/delete at position 0 is uniform; return dummy.next" },
      { name: "Iterative reversal", note: "prev/cur/nxt three-pointer dance; flip cur.next to prev each step" },
      { name: "Two-pointer (fast/slow)", note: "find middle, detect cycle, or n-th from end in one pass" },
      { name: "Merge with tail", note: "thread two lists by repeatedly attaching the smaller node to a moving tail" },
      { name: "Runner offset", note: "advance one pointer k nodes ahead, then move both to locate a relative position" },
    ],
    complexity: [
      { operation: "Access / search by value", time: "O(n)", space: "O(1)", note: "must walk from the head" },
      { operation: "Insert / delete at head", time: "O(1)", space: "O(1)", note: "just rewire pointers" },
      { operation: "Insert / delete given the node", time: "O(1)", space: "O(1)", note: "splice; doubly-linked needs no predecessor" },
      { operation: "Reverse whole list", time: "O(n)", space: "O(1)", note: "iterative three-pointer" },
    ],
    edgeCases: [
      "Empty list (head is None)",
      "Single node (head.next is None)",
      "Operating on the head itself (dummy node avoids special-casing)",
      "The last node's next must end as None or you create an accidental cycle",
      "Two-node list when reversing or swapping pairs",
      "Losing the rest of the list by reassigning next before saving it",
    ],
    commonMistakes: [
      "Reassigning cur.next before saving nxt = cur.next, orphaning the tail",
      "Forgetting to return dummy.next instead of the original head after edits",
      "Off-by-one in 'n-th from end': advance the lead pointer n+1 (with dummy) steps",
      "Creating a cycle by not setting the final node's next to None",
      "Mutating while traversing without keeping a prev pointer for deletion",
      "Assuming the list is non-empty; head can be None",
      "Comparing nodes by value when you mean identity (use 'is', not '==')",
    ],
    template: {
      language: "python",
      code: "class ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef reverse_list(head):\n    prev = None\n    cur = head\n    while cur:\n        nxt = cur.next   # save the rest before we overwrite the link\n        cur.next = prev  # flip the pointer backward\n        prev = cur       # advance prev\n        cur = nxt        # advance cur\n    return prev          # new head\n\ndef remove_nth_from_end(head, n):\n    dummy = ListNode(0, head)\n    fast = slow = dummy\n    for _ in range(n):       # open a gap of n nodes\n        fast = fast.next\n    while fast.next:         # walk both until fast hits the last node\n        fast, slow = fast.next, slow.next\n    slow.next = slow.next.next  # unlink the target\n    return dummy.next",
    },
    observations: [
      "A dummy/sentinel head removes nearly all head-edge special cases - reach for it first",
      "Drawing pointers and naming the save variable (nxt) before any reassignment prevents most bugs",
      "Reversal is the universal subroutine: palindrome check, k-group reversal, and reorder-list all reuse it",
      "Doubly linked lists trade one pointer for O(1) deletion given only the node - the basis of LRU caches",
      "Recursion on a list costs O(n) stack space; the iterative version is O(1)",
      "Many 'in place' list tricks are really: find middle, reverse second half, then merge",
    ],
    problems: [
      { title: "Reverse Linked List", url: "https://leetcode.com/problems/reverse-linked-list/", difficulty: "Easy", pattern: "iterative three-pointer reversal" },
      { title: "Merge Two Sorted Lists", url: "https://leetcode.com/problems/merge-two-sorted-lists/", difficulty: "Easy", pattern: "merge with a dummy tail" },
      { title: "Remove Nth Node From End of List", url: "https://leetcode.com/problems/remove-nth-node-from-end-of-list/", difficulty: "Medium", pattern: "two-pointer gap + dummy head" },
      { title: "Reorder List", url: "https://leetcode.com/problems/reorder-list/", difficulty: "Medium", pattern: "find middle, reverse second half, interleave" },
      { title: "Reverse Nodes in k-Group", url: "https://leetcode.com/problems/reverse-nodes-in-k-group/", difficulty: "Hard", pattern: "segmented reversal with group head/tail relinking" },
    ],
    videos: [
      { title: "Reverse Linked List - Iterative AND Recursive - Leetcode 206 - Python", url: "https://www.youtube.com/watch?v=G0_I-ZF0S38", source: "NeetCode" },
      { title: "L9. Reverse a LinkedList | Iterative and Recursive", url: "https://www.youtube.com/watch?v=D2vI2DNJGd8", source: "takeUforward (Striver)" },
      { title: "Linked List - Full Tutorial Playlist", url: "https://www.youtube.com/playlist?list=PLot-Xpze53leU0Ec0VkBhnf4npMRFiNcB", source: "NeetCode" },
    ],
    articles: [
      { title: "Linked List Data Structure", url: "https://www.geeksforgeeks.org/data-structures/linked-list/", source: "GeeksforGeeks" },
      { title: "Linked list", url: "https://en.wikipedia.org/wiki/Linked_list", source: "Wikipedia" },
      { title: "Reverse a Linked List - Tutorial", url: "https://takeuforward.org/data-structure/reverse-a-linked-list", source: "takeUforward" },
    ],
  },

  "fast-slow-pointers": {
    id: "fast-slow-pointers",
    name: "Fast & Slow Pointers",
    summary: "Two pointers at different speeds (Floyd's tortoise and hare) detect cycles and find midpoints in O(1) space.",
    overview:
      "## The core idea\n\nMove two pointers through a sequence at **different speeds** - typically `slow` one step and `fast` two steps. On a finite chain `fast` reaches the end first; if a **cycle** exists, `fast` laps the loop and inevitably collides with `slow`. Because the gap shrinks by one each tick inside the loop, they are guaranteed to meet. All of this uses **O(1) extra space**.\n\n## How to think\n\nAsk: **is there an implicit linked structure, even without ListNode?** Any function `i -> next(i)` defines one - array indices, digit-square sums (Happy Number), `nums[nums[i]]` (Find the Duplicate). If the sequence must eventually repeat (finite states), a cycle exists and Floyd applies. Then ask **what do I want**: just *does a cycle exist* (do they meet?), *where does it start* (reset one pointer to the head and walk both at speed 1 - they meet at the entrance), or *the midpoint* (when fast hits the end, slow is at the middle).\n\n## Variants\n\n- **Cycle detection** - do `slow` and `fast` ever meet?\n- **Cycle entry** - the reset trick finds the first node of the loop.\n- **Middle of list** - fast goes 2x; slow lands on the middle.\n- **Cycle length** - keep one pointer fixed at the meeting point and count until it returns.\n- **Abstract functional graphs** - Happy Number, Find the Duplicate Number.\n\nEdge insight: the **why** behind the reset trick is the distance algebra `2 * slow = fast`, which makes the head-to-entry distance equal the meeting-point-to-entry distance. Memorize the trick; the proof is the bonus.",
    recognition: [
      "Detect whether a linked list (or functional sequence) has a cycle",
      "Find where a cycle begins, or its length",
      "Find the middle node of a list in a single pass",
      "An O(1)-space requirement rules out a visited hash set",
      "Values define an implicit next(): nums[nums[i]], digit-square sums, etc.",
      "You must find a duplicate without modifying the array and without extra space",
    ],
    whenToUse: [
      "Cycle detection / cycle-start in linked lists or functional graphs",
      "Locating the midpoint to split a list (merge sort, palindrome, reorder)",
      "Finding the n-th-from-end via a fixed-gap fast pointer",
      "Duplicate-in-array problems reframed as cycle finding (Find the Duplicate Number)",
    ],
    whenNotToUse: [
      "The structure has no well-defined single 'next' per node (branching tree/graph - use DFS/BFS with visited)",
      "You can afford O(n) space and a hash set is clearer for the team",
      "You need all cycles or full graph structure, not just existence/entry",
      "Random access by index is what you actually need (plain indexing wins)",
    ],
    patterns: [
      { name: "Tortoise & hare (detect)", note: "slow += 1, fast += 2; a meeting means a cycle exists" },
      { name: "Find cycle entry", note: "after meeting, reset slow to head; advance both by 1; they meet at the loop start" },
      { name: "Find middle", note: "when fast or fast.next is None, slow is the middle node" },
      { name: "Cycle length", note: "fix one pointer at the meeting node, walk the other until it returns, counting steps" },
      { name: "Functional-graph cycle", note: "treat next(x) = f(x) (e.g. nums[x] or digit-square-sum) and apply Floyd" },
    ],
    complexity: [
      { operation: "Cycle detection", time: "O(n)", space: "O(1)", note: "fast traverses at most ~2n before meeting or ending" },
      { operation: "Find cycle start", time: "O(n)", space: "O(1)", note: "second phase walks at most n more steps" },
      { operation: "Find middle", time: "O(n)", space: "O(1)", note: "single pass with the 2x pointer" },
      { operation: "Cycle length", time: "O(n)", space: "O(1)", note: "one more loop around the cycle" },
    ],
    edgeCases: [
      "Empty list or single node with no cycle",
      "A self-loop (node points to itself)",
      "The whole list is one big cycle (head is the entry)",
      "Even vs odd length when defining 'middle' (slow lands on the second middle for even)",
      "fast.next can be None - check both fast and fast.next before fast = fast.next.next",
      "No cycle at all: fast simply reaches None and you must stop",
    ],
    commonMistakes: [
      "Checking only `while fast` and not `fast.next`, causing a None.next crash",
      "Advancing fast by two in one statement without verifying the intermediate node exists",
      "For cycle-start, resetting fast instead of slow, or forgetting both now move by 1",
      "Returning the meeting node as the cycle start (it is not - you must do the reset phase)",
      "Off-by-one on 'middle': decide up front whether you want the first or second middle",
      "Assuming a meeting implies a specific position; it only implies a cycle exists",
      "Using value equality to test 'same node' instead of identity",
    ],
    template: {
      language: "python",
      code: "def has_cycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            return True\n    return False\n\ndef cycle_start(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:            # collision inside the loop\n            slow = head             # reset one pointer to the head\n            while slow is not fast:  # both now move one step\n                slow = slow.next\n                fast = fast.next\n            return slow             # the cycle's entry node\n    return None\n\ndef middle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    return slow                      # second middle when length is even",
    },
    observations: [
      "Any finite sequence with a deterministic next() must eventually cycle - that is why Floyd works on arrays and number sequences too",
      "The reset trick rests on 2*slow = fast, which makes 'head to entry' equal 'meeting to entry'",
      "It trades a hash set's O(n) space for O(1), at the cost of a slightly subtle proof",
      "For the midpoint, starting fast at head vs head.next is what selects the first vs second middle",
      "Find the Duplicate Number is secretly cycle detection over the index graph i -> nums[i]",
      "Speeds other than 1 and 2 also work for detection but 1/2 is simplest and proves cleanly",
    ],
    problems: [
      { title: "Middle of the Linked List", url: "https://leetcode.com/problems/middle-of-the-linked-list/", difficulty: "Easy", pattern: "fast 2x to find midpoint" },
      { title: "Linked List Cycle", url: "https://leetcode.com/problems/linked-list-cycle/", difficulty: "Easy", pattern: "tortoise & hare existence test" },
      { title: "Happy Number", url: "https://leetcode.com/problems/happy-number/", difficulty: "Easy", pattern: "Floyd on the digit-square-sum function" },
      { title: "Linked List Cycle II", url: "https://leetcode.com/problems/linked-list-cycle-ii/", difficulty: "Medium", pattern: "detect then reset to find cycle entry" },
      { title: "Find the Duplicate Number", url: "https://leetcode.com/problems/find-the-duplicate-number/", difficulty: "Medium", pattern: "cycle finding over i -> nums[i]" },
    ],
    videos: [
      { title: "Linked List Cycle - Floyd's Tortoise and Hare - Leetcode 141 - Python", url: "https://www.youtube.com/watch?v=gBTe7lFR3vc", source: "NeetCode" },
      { title: "Find the Duplicate Number - Floyd's Cycle Detection - Leetcode 287 - Python", url: "https://www.youtube.com/watch?v=wjYnzkAhcNk", source: "NeetCode" },
      { title: "Advanced Algorithms 5 - Fast and Slow Pointers (course)", url: "https://neetcode.io/courses/advanced-algorithms/5", source: "NeetCode.io" },
    ],
    articles: [
      { title: "Floyd's Cycle Finding Algorithm", url: "https://www.geeksforgeeks.org/dsa/floyds-cycle-finding-algorithm/", source: "GeeksforGeeks" },
      { title: "Tortoise and Hare (cycle detection)", url: "https://cp-algorithms.com/others/tortoise_and_hare.html", source: "CP-Algorithms" },
      { title: "Cycle detection", url: "https://en.wikipedia.org/wiki/Cycle_detection", source: "Wikipedia" },
    ],
  },

  "recursion": {
    id: "recursion",
    name: "Recursion",
    summary: "A function that solves a problem by calling itself on smaller inputs, anchored by a base case.",
    overview:
      "## The core idea\n\n**Recursion** solves a problem by reducing it to one or more **smaller instances of the same problem**, then combining their answers. Every recursive function needs two parts: a **base case** that returns directly without recursing, and a **recursive case** that makes progress toward the base. The machine tracks the pending work for you on the **call stack**.\n\n## How to think\n\nDon't trace the whole tree of calls in your head - that way lies madness. Instead trust the **recursive leap of faith**: assume the function already works for smaller inputs, then ask only **(1)** what is the smallest input I can answer outright (base case), and **(2)** assuming `f(smaller)` is correct, how do I build `f(n)` from it? Verify two things: every path **shrinks** the input (or you get infinite recursion / stack overflow), and the base case is actually reached.\n\n## Variants\n\n- **Linear recursion** - one self-call (factorial, list length, simple traversal).\n- **Binary / multi-branch** - two-plus self-calls (tree traversal, Fibonacci, divide and conquer).\n- **Tail recursion** - the self-call is the last action; some languages optimize it to a loop (CPython does **not**).\n- **Backtracking** - recurse, choose, recurse, then undo the choice (subsets, permutations).\n- **Divide and conquer** - split, solve halves, merge (merge sort, Pow(x,n) by squaring).\n\nEdge insight: overlapping subproblems (plain Fibonacci recomputes the same call exponentially) are the signal to add **memoization** - that's the bridge from recursion to dynamic programming.",
    recognition: [
      "The problem is naturally defined in terms of itself (trees, nested structures, n!)",
      "You can describe the answer as 'combine answers of smaller pieces'",
      "Generating all combinations / permutations / subsets (backtracking)",
      "Divide-and-conquer is hinted: split in half, solve, merge",
      "Input is a tree, grid region, or nested list of unknown depth",
      "A problem has a clean inductive definition with a clear base case",
    ],
    whenToUse: [
      "Tree and graph traversal where each child is a sub-instance",
      "Divide-and-conquer algorithms (merge sort, quickselect, fast exponentiation)",
      "Enumerating combinatorial objects via backtracking",
      "Problems with overlapping subproblems, paired with memoization (top-down DP)",
    ],
    whenNotToUse: [
      "Recursion depth can exceed the stack limit (deep/linear chains - CPython defaults to ~1000)",
      "A simple loop is clearer and the call overhead matters in a hot path",
      "Tail-recursive logic in Python (no TCO; rewrite as a loop)",
      "The same subproblems recur and you have not added memoization (exponential blowup)",
    ],
    patterns: [
      { name: "Base + recursive case", note: "smallest answerable input returns directly; otherwise reduce and recurse" },
      { name: "Divide and conquer", note: "split into independent halves, solve each, merge the results" },
      { name: "Backtracking", note: "make a choice, recurse, then undo it before trying the next" },
      { name: "Memoization", note: "cache results by argument to collapse overlapping subproblems to O(unique states)" },
      { name: "Tail recursion -> loop", note: "when the call is the last step, convert to iteration in languages without TCO" },
    ],
    complexity: [
      { operation: "Linear recursion (factorial)", time: "O(n)", space: "O(n)", note: "n stack frames" },
      { operation: "Binary recursion, naive Fib", time: "O(2^n)", space: "O(n)", note: "exponential without memoization" },
      { operation: "Divide & conquer (merge sort)", time: "O(n log n)", space: "O(n)", note: "log n depth, merge work per level" },
      { operation: "Fast exponentiation", time: "O(log n)", space: "O(log n)", note: "halve the exponent each call" },
      { operation: "Memoized DP", time: "O(states)", space: "O(states)", note: "each unique subproblem solved once" },
    ],
    edgeCases: [
      "Empty input / n = 0 / null node - usually the base case itself",
      "Single element or single node",
      "Negative or zero exponents in Pow(x, n) (n can be INT_MIN)",
      "Very deep input causing RecursionError in Python",
      "Duplicate elements in subsets/permutations needing dedup (sort + skip)",
      "Forgetting to return the recursive result (function returns None)",
    ],
    commonMistakes: [
      "Missing or unreachable base case -> infinite recursion / stack overflow",
      "Not actually shrinking the input on every path",
      "Forgetting to return or accumulate the recursive call's result",
      "Mutating shared state in backtracking without undoing it (no clean revert)",
      "Recomputing overlapping subproblems instead of memoizing (exponential time)",
      "Assuming Python optimizes tail calls - it does not",
      "Off-by-one in the split point of divide-and-conquer (mid handling)",
    ],
    template: {
      language: "python",
      code: "# 1) Divide & conquer: fast exponentiation, O(log n)\ndef my_pow(x, n):\n    if n < 0:\n        x, n = 1 / x, -n\n    if n == 0:          # base case\n        return 1.0\n    half = my_pow(x, n // 2)\n    return half * half * (x if n % 2 else 1)\n\n# 2) Backtracking: all subsets\ndef subsets(nums):\n    res, path = [], []\n    def backtrack(i):\n        if i == len(nums):       # base case: a full decision sequence\n            res.append(path[:])  # copy the current path\n            return\n        backtrack(i + 1)         # choice: skip nums[i]\n        path.append(nums[i])     # choice: take nums[i]\n        backtrack(i + 1)\n        path.pop()               # undo before returning\n    backtrack(0)\n    return res",
    },
    observations: [
      "Trust the leap of faith: design the recursive step assuming smaller calls are already correct",
      "Recursion depth is real memory - each frame costs stack space (O(depth))",
      "Backtracking is recursion plus an explicit undo; the symmetry of do/undo is the correctness check",
      "Overlapping subproblems are the cue to memoize - that single change is what makes top-down DP",
      "Any recursion can be rewritten iteratively with an explicit stack when depth is a risk",
      "Tree problems are recursion's home turf: base case is the null child, combine is the parent's logic",
    ],
    problems: [
      { title: "Fibonacci Number", url: "https://leetcode.com/problems/fibonacci-number/", difficulty: "Easy", pattern: "linear recurrence -> memoization" },
      { title: "Pow(x, n)", url: "https://leetcode.com/problems/powx-n/", difficulty: "Medium", pattern: "divide & conquer fast exponentiation" },
      { title: "Subsets", url: "https://leetcode.com/problems/subsets/", difficulty: "Medium", pattern: "backtracking: take / skip each element" },
      { title: "Permutations", url: "https://leetcode.com/problems/permutations/", difficulty: "Medium", pattern: "backtracking with used-set / swap" },
      { title: "Merge k Sorted Lists", url: "https://leetcode.com/problems/merge-k-sorted-lists/", difficulty: "Hard", pattern: "divide & conquer pairwise merge" },
    ],
    videos: [
      { title: "Recursion in Programming - Full Course", url: "https://www.youtube.com/watch?v=IJDJ0kBx2LM", source: "freeCodeCamp" },
      { title: "Reverse Linked List - Iterative AND Recursive - Leetcode 206 - Python", url: "https://www.youtube.com/watch?v=G0_I-ZF0S38", source: "NeetCode" },
      { title: "Abdul Bari - Algorithms (recursion, divide & conquer)", url: "https://www.youtube.com/playlist?list=PLDN4rrl48XKpZkf03iYFl-O29szjTrs_O", source: "Abdul Bari" },
    ],
    articles: [
      { title: "Introduction to Recursion", url: "https://www.geeksforgeeks.org/introduction-to-recursion-data-structure-and-algorithm-tutorials/", source: "GeeksforGeeks" },
      { title: "Recursion (computer science)", url: "https://en.wikipedia.org/wiki/Recursion_(computer_science)", source: "Wikipedia" },
      { title: "Understanding Recursion in Programming", url: "https://www.freecodecamp.org/news/understanding-recursion-in-programming/", source: "freeCodeCamp" },
    ],
  },

  "trees": {
    id: "trees",
    name: "Trees",
    summary: "Hierarchical nodes with one root and no cycles - the natural home of recursion.",
    overview:
      "## The core idea\n\nA **tree** is a connected, acyclic structure: one **root**, every other node has exactly one parent, and nodes branch into children. A **binary tree** caps that at a left and right child. Because each subtree is itself a tree, trees and **recursion** fit like a hand in a glove - the base case is the empty (null) child, and the recursive case combines results from the children.\n\n## How to think\n\nFor almost any tree problem, pick a traversal and decide **what each node returns to its parent**. Two mental templates cover most of it. **DFS (recursion)**: 'solve left, solve right, then combine here.' Ask what value a node must hand up (a height, a sum, a boolean) and what it must check locally. **BFS (queue)**: process level by level when the answer depends on depth or you want the shallowest result. Choosing pre/in/post-order is just choosing *when* you act relative to visiting the children.\n\n## Variants\n\n- **Traversals** - preorder (root first), inorder (left-root-right), postorder (children first), and level-order (BFS).\n- **DFS vs BFS** - DFS for path/height/aggregate questions; BFS for level/shortest-depth questions.\n- **Divide-and-conquer on trees** - each node returns a summary; a global variable tracks the best (diameter, max path sum).\n- **Construction** - build from traversals or sorted input.\n\nEdge insight: the empty tree (`root is None`) is the single most important base case - handle it first, every time, and most of the recursion writes itself. The other trap is a skewed tree making recursion depth O(n), not O(log n).",
    recognition: [
      "Input is a hierarchy: root with children, or a binary tree node with left/right",
      "The answer for a node depends on its subtrees (height, sum, depth, validity)",
      "You need a traversal order: preorder/inorder/postorder/level-order",
      "Level-by-level processing or shallowest-node questions (BFS)",
      "'Path', 'depth', 'ancestor', 'lowest common ancestor', or 'diameter' appear",
      "Building or serializing a tree from its traversal output",
    ],
    whenToUse: [
      "Modeling hierarchy: file systems, org charts, expression trees, the DOM",
      "Recursive divide-and-conquer where each subtree yields a partial answer",
      "Level-order / shortest-depth queries via BFS",
      "Range or ordered queries when the tree is a BST (then use BST ordering)",
    ],
    whenNotToUse: [
      "Data is flat with no hierarchy (an array or hash map is simpler)",
      "You need O(1) random access by index (use an array)",
      "The structure has cycles or many parents (that is a general graph, not a tree)",
      "A balanced order-statistics structure is required but you can't guarantee balance (use a balanced BST / heap)",
    ],
    patterns: [
      { name: "DFS preorder", note: "act on the node, then recurse left, then right (copy/serialize)" },
      { name: "DFS inorder", note: "left, node, right - yields sorted order on a BST" },
      { name: "DFS postorder", note: "left, right, then node - needed when a node depends on child results (height, diameter)" },
      { name: "BFS level-order", note: "queue of nodes; process one level at a time for depth-based answers" },
      { name: "Return-and-track", note: "node returns a local summary while a closure variable records the global best" },
    ],
    complexity: [
      { operation: "Any traversal (visit all nodes)", time: "O(n)", space: "O(h)", note: "h = height; recursion stack" },
      { operation: "DFS recursion stack", time: "O(n)", space: "O(h)", note: "O(log n) balanced, O(n) skewed" },
      { operation: "BFS level-order", time: "O(n)", space: "O(w)", note: "w = max width of a level" },
      { operation: "Height / depth", time: "O(n)", space: "O(h)", note: "postorder combine" },
    ],
    edgeCases: [
      "Empty tree (root is None) - the universal base case",
      "Single node (it is both root and leaf)",
      "Skewed tree degenerating to a list (height = n, stack risk)",
      "Negative node values when summing paths (don't assume positives)",
      "Distinguishing a leaf (no children) from an internal node with one child",
      "Duplicate values when matching subtrees or building from traversals",
    ],
    commonMistakes: [
      "Forgetting the root-is-None base case, crashing on .left / .val",
      "Confusing the three DFS orders - pick by when the node's work must happen",
      "Using DFS where the answer is depth-based (BFS is the natural fit)",
      "Ignoring the skewed-tree case and assuming O(log n) depth",
      "Counting nodes vs edges inconsistently when measuring height/diameter",
      "Mutating a shared accumulator across siblings without resetting it",
      "Assuming a binary tree is a BST and relying on ordering that isn't there",
    ],
    template: {
      language: "python",
      code: "from collections import deque\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val, self.left, self.right = val, left, right\n\n# DFS postorder: max depth (each node returns its height)\ndef max_depth(root):\n    if not root:                 # base case: empty subtree\n        return 0\n    return 1 + max(max_depth(root.left), max_depth(root.right))\n\n# BFS level-order: list of values per level\ndef level_order(root):\n    if not root:\n        return []\n    res, q = [], deque([root])\n    while q:\n        level = []\n        for _ in range(len(q)):  # fix the count for this level\n            node = q.popleft()\n            level.append(node.val)\n            if node.left:  q.append(node.left)\n            if node.right: q.append(node.right)\n        res.append(level)\n    return res",
    },
    observations: [
      "Trees are recursion's natural habitat: the null child is your base case nearly every time",
      "Postorder is the workhorse when a node's answer depends on its children's answers",
      "Inorder traversal of a BST emits values in sorted order - a frequently exploited fact",
      "DFS uses O(h) stack; BFS uses O(w) queue - the better choice depends on the tree's shape",
      "Diameter and max-path-sum share one trick: return the best single-branch value, update a global best with the through-node value",
      "Recursion depth equals height; a balanced tree gives O(log n), a skewed one O(n)",
    ],
    problems: [
      { title: "Maximum Depth of Binary Tree", url: "https://leetcode.com/problems/maximum-depth-of-binary-tree/", difficulty: "Easy", pattern: "postorder height combine" },
      { title: "Invert Binary Tree", url: "https://leetcode.com/problems/invert-binary-tree/", difficulty: "Easy", pattern: "DFS swap children" },
      { title: "Binary Tree Level Order Traversal", url: "https://leetcode.com/problems/binary-tree-level-order-traversal/", difficulty: "Medium", pattern: "BFS with per-level batching" },
      { title: "Diameter of Binary Tree", url: "https://leetcode.com/problems/diameter-of-binary-tree/", difficulty: "Easy", pattern: "return height, track global best through node" },
      { title: "Binary Tree Maximum Path Sum", url: "https://leetcode.com/problems/binary-tree-maximum-path-sum/", difficulty: "Hard", pattern: "postorder return best branch, update global max" },
    ],
    videos: [
      { title: "Invert Binary Tree - Depth First Search - Leetcode 226", url: "https://www.youtube.com/watch?v=OnSn2XEQ4MY", source: "NeetCode" },
      { title: "Trees - Full Tutorial Playlist", url: "https://www.youtube.com/playlist?list=PLot-Xpze53ldg4pN6PfzoJY7KsKcxF1jg", source: "NeetCode" },
      { title: "Binary Search Tree Traversals (Tree DSA course)", url: "https://www.youtube.com/watch?v=k7GkEbECZK0", source: "WilliamFiset" },
    ],
    articles: [
      { title: "Introduction to Tree Data Structure", url: "https://www.geeksforgeeks.org/introduction-to-tree-data-structure-and-algorithm-tutorials/", source: "GeeksforGeeks" },
      { title: "Tree traversal", url: "https://en.wikipedia.org/wiki/Tree_traversal", source: "Wikipedia" },
      { title: "Binary Tree (Tree data structure)", url: "https://en.wikipedia.org/wiki/Binary_tree", source: "Wikipedia" },
    ],
  },

  "bst": {
    id: "bst",
    name: "Binary Search Trees",
    summary: "An ordered binary tree where left < node < right, giving O(h) search, insert, and delete.",
    overview:
      "## The core idea\n\nA **binary search tree (BST)** is a binary tree with an ordering invariant: for every node, **all values in its left subtree are smaller and all values in its right subtree are larger**. That invariant lets you binary-search the tree - compare against the current node and descend into exactly one subtree - giving **O(h)** search, insert, and delete, where `h` is the height.\n\n## How to think\n\nAt every node you make a **three-way decision**: equal (found / act here), smaller (go left), larger (go right). The whole power of a BST is that one comparison eliminates an entire subtree. Two facts unlock most problems: **(1)** an **inorder traversal visits values in sorted order**, so 'k-th smallest', 'validate', and 'range sum' reduce to inorder logic; **(2)** because of ordering, lowest-common-ancestor and search don't need to explore both sides - they pick a direction.\n\n## Variants\n\n- **Search / insert** - descend by comparison; insert at the null spot you fall off.\n- **Delete** - three cases: leaf (drop it), one child (splice), two children (replace with inorder successor/predecessor).\n- **Inorder-powered queries** - k-th smallest, validate BST, two-sum, range sum.\n- **Balanced BSTs** - AVL / Red-Black keep `h = O(log n)`; build a balanced BST from a sorted array by picking the middle as root.\n\nEdge insight: **O(h) is only O(log n) if the tree is balanced.** Inserting sorted data into a plain BST degenerates it into a linked list with O(n) operations - the motivation for self-balancing trees. When validating, track an open `(low, high)` interval, not just the parent, or you'll wrongly accept a value that beats its parent but violates a grandparent.",
    recognition: [
      "Data is kept sorted and you need fast search/insert/delete by value",
      "The phrase 'binary search tree' or a stated left < node < right invariant",
      "k-th smallest / largest, or successor / predecessor queries",
      "Range queries: count or sum values in [lo, hi]",
      "Lowest common ancestor where ordering lets you skip a subtree",
      "Validate that a binary tree respects the ordering invariant",
    ],
    whenToUse: [
      "Maintaining a dynamic sorted set with insert, delete, and lookup by value",
      "Order-statistics and range queries when the tree is balanced",
      "Finding successor / predecessor or k-th smallest efficiently",
      "Converting between sorted arrays and balanced trees",
    ],
    whenNotToUse: [
      "Inserts arrive nearly sorted and you can't self-balance (degenerates to O(n))",
      "A plain hash set/map suffices and you don't need ordering (hash is O(1) average)",
      "You only need the min or max repeatedly (a heap is simpler)",
      "The data is static - sort an array once and binary-search it instead",
    ],
    patterns: [
      { name: "Three-way descent", note: "compare target to node; go left if smaller, right if larger, stop if equal" },
      { name: "Inorder = sorted", note: "left, node, right yields ascending order; basis of k-th smallest and validation" },
      { name: "Interval validation", note: "carry (low, high) bounds down; each node must lie strictly inside" },
      { name: "Delete by successor", note: "two-child node: copy inorder successor's value, then delete it from the right subtree" },
      { name: "Balanced build", note: "from a sorted array, pick the middle as root and recurse on halves" },
    ],
    complexity: [
      { operation: "Search / insert / delete", time: "O(h)", space: "O(h)", note: "h = log n balanced, n skewed" },
      { operation: "Inorder traversal", time: "O(n)", space: "O(h)", note: "visits all nodes in sorted order" },
      { operation: "k-th smallest", time: "O(h + k)", space: "O(h)", note: "inorder, stop after k" },
      { operation: "Lowest common ancestor", time: "O(h)", space: "O(1)", note: "descend by comparing both targets" },
    ],
    edgeCases: [
      "Empty tree (root is None)",
      "Inserting a duplicate - decide your policy (reject, or send left/right consistently)",
      "Deleting the root, a leaf, or a single-child node (three distinct cases)",
      "Deleting a node with two children (use successor or predecessor)",
      "A degenerate skewed tree where h = n",
      "Validation: equal values - the invariant is usually strict (< and >), so reject equals",
    ],
    commonMistakes: [
      "Validating against only the parent, not an inherited (low, high) range",
      "Allowing equal values when the invariant is strict (or vice versa) inconsistently",
      "Mishandling the two-children delete case (forgetting to remove the successor)",
      "Assuming O(log n) without guaranteeing balance",
      "Using int min/max sentinels that overflow at the extremes (use None or float('inf'))",
      "Returning early in LCA before checking the equal/split case",
      "Doing a full O(n) scan when a directed O(h) descent suffices",
    ],
    template: {
      language: "python",
      code: "class TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val, self.left, self.right = val, left, right\n\ndef search_bst(root, target):\n    while root and root.val != target:\n        root = root.left if target < root.val else root.right\n    return root\n\ndef insert_bst(root, val):\n    if not root:\n        return TreeNode(val)\n    if val < root.val:\n        root.left = insert_bst(root.left, val)\n    else:\n        root.right = insert_bst(root.right, val)\n    return root\n\ndef is_valid_bst(root):\n    def ok(node, low, high):\n        if not node:\n            return True\n        if not (low < node.val < high):   # strict interval\n            return False\n        return ok(node.left, low, node.val) and ok(node.right, node.val, high)\n    return ok(root, float('-inf'), float('inf'))",
    },
    observations: [
      "Inorder traversal of a BST is sorted - memorize this; it answers k-th smallest, validate, and range queries",
      "Search is binary search on a tree: one comparison discards an entire subtree",
      "O(h) is only O(log n) when balanced; sorted inserts make a plain BST a linked list",
      "Validation needs an inherited (low, high) interval, not just a parent comparison",
      "The inorder successor of a node is the leftmost node of its right subtree",
      "Picking the middle of a sorted array as the root recursively builds a height-balanced BST",
    ],
    problems: [
      { title: "Search in a Binary Search Tree", url: "https://leetcode.com/problems/search-in-a-binary-search-tree/", difficulty: "Easy", pattern: "three-way descent" },
      { title: "Convert Sorted Array to Binary Search Tree", url: "https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/", difficulty: "Easy", pattern: "middle-as-root balanced build" },
      { title: "Lowest Common Ancestor of a Binary Search Tree", url: "https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/", difficulty: "Medium", pattern: "descend by comparing both targets" },
      { title: "Kth Smallest Element in a BST", url: "https://leetcode.com/problems/kth-smallest-element-in-a-bst/", difficulty: "Medium", pattern: "inorder traversal, stop at k" },
      { title: "Validate Binary Search Tree", url: "https://leetcode.com/problems/validate-binary-search-tree/", difficulty: "Medium", pattern: "inherited (low, high) interval check" },
    ],
    videos: [
      { title: "Validate Binary Search Tree - Leetcode 98 - Trees (Python)", url: "https://www.youtube.com/watch?v=nTXuWuqIka4", source: "NeetCode" },
      { title: "Binary Search Tree Introduction", url: "https://www.youtube.com/watch?v=JfSdGQdAzq8", source: "WilliamFiset" },
      { title: "Binary Search Tree Traversals", url: "https://www.youtube.com/watch?v=k7GkEbECZK0", source: "WilliamFiset" },
    ],
    articles: [
      { title: "Introduction to Binary Search Tree", url: "https://www.geeksforgeeks.org/binary-search-tree-data-structure/", source: "GeeksforGeeks" },
      { title: "Binary search tree", url: "https://en.wikipedia.org/wiki/Binary_search_tree", source: "Wikipedia" },
      { title: "Validate Binary Search Tree - In-Depth Explanation", url: "https://algo.monster/liteproblems/98", source: "AlgoMonster" },
    ],
  },
};
