// Concepts: Prefix Sum · Binary Search · Sorting · Stack · Monotonic Stack · Queue
export const BATCH = {
  "prefix-sum": {
    id: "prefix-sum",
    name: "Prefix Sum",
    summary: "Precompute cumulative totals so any range query collapses to one subtraction.",
    overview:
      "## The core idea\n\nA **prefix sum** array stores the running total up to each index: `pre[i] = nums[0] + ... + nums[i-1]`. Once built in `O(n)`, the sum of any subarray `[l, r]` is just `pre[r+1] - pre[l]` in `O(1)`. You trade a one-time linear pass for constant-time range queries.\n\n## How to think\n\nAsk: *am I being asked the same kind of range/aggregate question many times, or about many subarrays?* If a brute-force solution recomputes overlapping sums, a prefix array removes the repeated work. The mental pivot for the hardest variant is: **a subarray sum equals `k` iff `pre[r] - pre[l] == k`**, i.e. `pre[l] == pre[r] - k`. So instead of scanning all `l`, store seen prefix values in a hash map and look up `pre[r] - k` as you go. This turns an `O(n^2)` pair search into one `O(n)` pass.\n\n## Variants\n\n- **Static range query**: build `pre` once, answer `[l, r]` sums in `O(1)`.\n- **Count subarrays with sum k**: hash map of prefix-sum frequencies; works with negatives where sliding window fails.\n- **Difference array**: the inverse — add a value over a range with two point updates, then prefix-sum to materialize.\n- **2D prefix sum**: inclusion-exclusion for submatrix sums.\n\n## Edge thinking\n\nUse a length `n+1` array with `pre[0] = 0` so the empty prefix exists; this kills off-by-one bugs. For the hash-map count, **seed `{0: 1}`** before the loop so subarrays starting at index 0 are counted.",
    recognition: [
      "Many range-sum / range-aggregate queries over a fixed array",
      "'Count the number of subarrays whose sum equals k' (especially with negative numbers)",
      "Find a subarray with a given sum, or the longest/shortest such subarray",
      "Submatrix sum queries on a 2D grid",
      "Range update then query, or many overlapping increments (difference array)",
      "Equal counts of two symbols in a subarray (map one to +1, the other to -1)",
      "Answer depends on cumulative quantity (running balance, parity, score) at two indices",
    ],
    whenToUse: [
      "The array is static and you must answer repeated range queries fast",
      "You need to count or locate subarrays by sum and the array contains negatives",
      "You can rephrase a window condition as 'difference of two cumulative values'",
      "You must apply many range increments offline, then read final values once",
    ],
    whenNotToUse: [
      "The array is updated between queries (use a Fenwick/segment tree for mutable ranges)",
      "All values are positive and you want a window by sum — a sliding window is simpler and O(1) space",
      "You need the max/min over a range, not a sum (prefix max only works for prefixes, not arbitrary subranges)",
    ],
    patterns: [
      { name: "Static range query", note: "build pre[i+1]=pre[i]+nums[i]; sum[l,r]=pre[r+1]-pre[l]" },
      { name: "Count subarrays = k", note: "hash map of prefix-sum counts; add seen[pre - k], seed {0:1}" },
      { name: "Difference array", note: "diff[l]+=v, diff[r+1]-=v; prefix-sum diff to get final array" },
      { name: "2D prefix sum", note: "P[i][j] via inclusion-exclusion; submatrix sum in O(1)" },
      { name: "Prefix XOR / parity", note: "same trick with XOR or +1/-1 mapping for balance problems" },
    ],
    complexity: [
      { operation: "Build prefix array", time: "O(n)", space: "O(n)", note: "one pass; can be O(1) extra if in place" },
      { operation: "Single range-sum query", time: "O(1)", space: "O(1)", note: "two array reads + subtraction" },
      { operation: "Count subarrays with sum k", time: "O(n)", space: "O(n)", note: "hash map of prefix counts" },
      { operation: "Build 2D prefix sum", time: "O(m*n)", space: "O(m*n)", note: "submatrix query then O(1)" },
    ],
    edgeCases: [
      "Empty array or single element — pre has length n+1, pre[0]=0",
      "Subarray that starts at index 0 — needs the seeded {0:1} entry in the count map",
      "Negative numbers — sliding window breaks, prefix+hashmap still works",
      "k = 0 — subarrays summing to zero (two equal prefix sums)",
      "All elements zero — many equal prefixes, watch frequency accumulation",
      "Inclusive vs exclusive index conventions on [l, r]",
    ],
    commonMistakes: [
      "Forgetting to seed the count map with {0: 1}, missing subarrays from index 0",
      "Off-by-one: mixing pre[r]-pre[l] with pre[r+1]-pre[l] conventions",
      "Trying a sliding window on a sum problem that has negative numbers",
      "Adding the current prefix to the map before querying it (double counts when k=0)",
      "Using prefix sums for range max/min — only valid for sums/XOR-like associative ops",
      "Allocating an n-length prefix and then indexing it as if it were n+1",
    ],
    template: {
      language: "python",
      code: "from collections import defaultdict\n\ndef range_sums(nums):\n    # pre[i] = sum of first i elements; pre[0] = 0\n    pre = [0] * (len(nums) + 1)\n    for i, x in enumerate(nums):\n        pre[i + 1] = pre[i] + x\n    # sum of nums[l..r] inclusive:\n    def query(l, r):\n        return pre[r + 1] - pre[l]\n    return query\n\ndef subarray_sum_equals_k(nums, k):\n    # count subarrays whose sum == k (handles negatives)\n    count = 0\n    cur = 0\n    seen = defaultdict(int)\n    seen[0] = 1                 # empty prefix\n    for x in nums:\n        cur += x\n        count += seen[cur - k]  # query BEFORE inserting\n        seen[cur] += 1\n    return count",
    },
    observations: [
      "The hash-map count trick is the prefix-sum 'one weird trick' — most medium prefix problems reduce to it",
      "Query the map before inserting the current prefix, or k=0 self-counts",
      "Prefix sums make sliding window unnecessary when negatives are allowed",
      "A difference array is just a prefix sum run backwards: range update in O(1), reconstruct in O(n)",
      "Map values to +1/-1 to turn 'equal count of A and B' into 'subarray sum = 0'",
      "2D prefix sums use inclusion-exclusion: P[r2][c2]-P[r1][c2]-P[r2][c1]+P[r1][c1]",
    ],
    problems: [
      { title: "Range Sum Query - Immutable", url: "https://leetcode.com/problems/range-sum-query-immutable/", difficulty: "Easy", pattern: "static 1D prefix array, O(1) query" },
      { title: "Find Pivot Index", url: "https://leetcode.com/problems/find-pivot-index/", difficulty: "Easy", pattern: "left sum vs total - left - self" },
      { title: "Subarray Sum Equals K", url: "https://leetcode.com/problems/subarray-sum-equals-k/", difficulty: "Medium", pattern: "prefix sum + hash map count, handles negatives" },
      { title: "Product of Array Except Self", url: "https://leetcode.com/problems/product-of-array-except-self/", difficulty: "Medium", pattern: "prefix and suffix products (no division)" },
      { title: "Range Sum Query 2D - Immutable", url: "https://leetcode.com/problems/range-sum-query-2d-immutable/", difficulty: "Medium", pattern: "2D prefix sum with inclusion-exclusion" },
    ],
    videos: [
      { title: "Subarray Sum Equals K - Prefix Sums - Leetcode 560 - Python", url: "https://www.youtube.com/watch?v=fFVZt-6sgyo", source: "NeetCode" },
      { title: "Prefix Sum (Advanced Algorithms course)", url: "https://neetcode.io/courses/advanced-algorithms/4", source: "NeetCode" },
      { title: "Count Subarray Sum Equals K (Prefix Sum + HashMap)", url: "https://www.youtube.com/watch?v=xvNwoz-ufXA", source: "takeUforward (Striver)" },
    ],
    articles: [
      { title: "Prefix Sum Array - Implementation and Applications", url: "https://www.geeksforgeeks.org/prefix-sum-array-implementation-applications-competitive-programming/", source: "GeeksforGeeks" },
      { title: "Prefix Sums and Difference Arrays", url: "https://usaco.guide/silver/prefix-sums", source: "USACO Guide" },
      { title: "Count Subarray Sum Equals K", url: "https://takeuforward.org/arrays/count-subarray-sum-equals-k", source: "takeUforward" },
    ],
  },

  "binary-search": {
    id: "binary-search",
    name: "Binary Search",
    summary: "Halve a monotonic search space each step to find an answer in O(log n).",
    overview:
      "## The core idea\n\nBinary search repeatedly halves a range as long as the space is **monotonic**: there is some boundary where a yes/no predicate flips exactly once. Classic form searches a sorted array for a target; the powerful form searches over an *answer space*.\n\n## How to think\n\nDon't think 'is the array sorted?' — think **'is there a predicate `f(x)` that is False, False, ..., True, True?'** If you can define such a monotone predicate, you can binary-search the smallest `x` with `f(x) == True`. This reframing unlocks 'binary search on the answer': for problems like *minimum capacity to ship in D days* or *smallest divisor*, you binary-search the answer value and use a feasibility check as the predicate.\n\nPick **one invariant and stick to it**. The most robust is the half-open `[lo, hi)` 'find leftmost True' template: it avoids most off-by-one and infinite-loop traps because the loop is `while lo < hi` and you never compute `mid` you've already rejected.\n\n## Variants\n\n- **Exact match** in a sorted array.\n- **Lower/upper bound**: first index `>=` / `>` target (use `bisect`).\n- **Binary search on answer**: search a value range, predicate = 'is this feasible?'.\n- **Rotated / unsorted-but-monotone**: rotated sorted array, peak finding, search in a 2D matrix.\n\n## Edge thinking\n\nUse `mid = lo + (hi - lo) // 2` to dodge overflow (harmless in Python but a good habit), and make sure the loop **strictly shrinks** every iteration — assigning `lo = mid` instead of `lo = mid + 1` is the classic infinite loop.",
    recognition: [
      "Input is sorted (or can be sorted cheaply) and you need a fast lookup",
      "You need the first/last position satisfying a condition (lower/upper bound)",
      "The answer is a number in a known range and feasibility is monotone in that number",
      "Phrases like 'minimum/maximum X such that ...' or 'smallest capacity/speed/divisor'",
      "O(log n) is required by the constraints (n up to 1e9, or a tight per-query limit)",
      "A rotated sorted array, a peak, or a row/col-sorted matrix",
      "You can write a check(x) that is monotonically true past some threshold",
    ],
    whenToUse: [
      "Searching a sorted array or a monotone function for a boundary",
      "Minimizing/maximizing an answer with a monotone feasibility predicate",
      "Replacing an O(n) scan over a sorted structure with O(log n) lookups",
      "Finding insertion points, counts of elements <= x, or kth-order statistics on sorted data",
    ],
    whenNotToUse: [
      "The data is unsorted and sorting would dominate the cost without other benefit",
      "The predicate is not monotone (true/false pattern is not a single flip)",
      "You only need one pass and a hash map gives O(1) lookup anyway",
    ],
    patterns: [
      { name: "Exact match", note: "classic sorted-array search; return index or -1" },
      { name: "Leftmost True (lower bound)", note: "[lo,hi) half-open; smallest x with f(x) true" },
      { name: "Binary search on answer", note: "search value range; predicate = feasibility check" },
      { name: "Rotated / peak", note: "decide which half is sorted, or which side rises" },
      { name: "2D matrix search", note: "treat row-sorted matrix as 1D, or step from a corner" },
    ],
    complexity: [
      { operation: "Search sorted array", time: "O(log n)", space: "O(1)", note: "iterative, no recursion stack" },
      { operation: "Binary search on answer", time: "O(log(range) * check)", space: "O(1)", note: "check is feasibility cost" },
      { operation: "Build then many queries", time: "O(n log n + q log n)", space: "O(1)", note: "sort once, query many" },
    ],
    edgeCases: [
      "Empty array — return -1 / lo without touching mid",
      "Target smaller than all / larger than all elements",
      "Duplicates — exact search may need leftmost or rightmost occurrence",
      "Single element range lo == hi",
      "Answer range where lo itself is the answer (predicate true at the very start)",
      "Even-length arrays where mid biases low — fine if loop shrinks correctly",
    ],
    commonMistakes: [
      "Infinite loop from lo = mid (must be lo = mid + 1) or wrong loop condition",
      "Mixing inclusive [lo, hi] and half-open [lo, hi) conventions mid-solution",
      "Returning mid when the problem wants a boundary (leftmost/rightmost) index",
      "Computing mid as (lo + hi) // 2 in languages with int overflow",
      "Assuming the predicate is monotone without proving it",
      "Off-by-one in lower vs upper bound (>= vs >)",
    ],
    template: {
      language: "python",
      code: "def lower_bound(arr, target):\n    # smallest index i with arr[i] >= target (half-open [lo, hi))\n    lo, hi = 0, len(arr)\n    while lo < hi:\n        mid = lo + (hi - lo) // 2\n        if arr[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n    return lo                      # insertion point\n\ndef search_on_answer(lo, hi, feasible):\n    # smallest x in [lo, hi] with feasible(x) True (False..False,True..True)\n    while lo < hi:\n        mid = lo + (hi - lo) // 2\n        if feasible(mid):\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo",
    },
    observations: [
      "If you can phrase the question as a monotone predicate, you can binary-search it — even with no array in sight",
      "The half-open leftmost-True template handles ~90% of variants; memorize one, derive the rest",
      "Python's bisect_left / bisect_right are battle-tested lower/upper bound — use them in interviews when allowed",
      "Loop must strictly shrink the range each iteration or it never terminates",
      "Searching on the answer changes complexity to O(log(value range) * cost-of-check)",
      "For rotated arrays, exactly one half is always sorted — compare mid to lo/hi to find it",
    ],
    problems: [
      { title: "Binary Search", url: "https://leetcode.com/problems/binary-search/", difficulty: "Easy", pattern: "classic exact match in sorted array" },
      { title: "Search Insert Position", url: "https://leetcode.com/problems/search-insert-position/", difficulty: "Easy", pattern: "lower bound / insertion point" },
      { title: "Find First and Last Position of Element in Sorted Array", url: "https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/", difficulty: "Medium", pattern: "leftmost and rightmost via two bounds" },
      { title: "Search in Rotated Sorted Array", url: "https://leetcode.com/problems/search-in-rotated-sorted-array/", difficulty: "Medium", pattern: "identify the sorted half each step" },
      { title: "Koko Eating Bananas", url: "https://leetcode.com/problems/koko-eating-bananas/", difficulty: "Medium", pattern: "binary search on the answer with feasibility check" },
    ],
    videos: [
      { title: "Binary Search - Leetcode 704 - Python", url: "https://www.youtube.com/watch?v=s4DPM8ct1pI", source: "NeetCode" },
      { title: "BS-1. Binary Search Introduction | Iterative & Recursive", url: "https://www.youtube.com/watch?v=MHf6awe89xw", source: "takeUforward (Striver)" },
      { title: "Binary Search Striver (full playlist)", url: "https://www.youtube.com/playlist?list=PLgUwDviBIf0pMFMWuuvDNMAkoQFi-h0ZF", source: "takeUforward (Striver)" },
    ],
    articles: [
      { title: "Binary Search", url: "https://www.geeksforgeeks.org/binary-search/", source: "GeeksforGeeks" },
      { title: "Binary Search", url: "https://cp-algorithms.com/num_methods/binary_search.html", source: "CP-Algorithms" },
      { title: "Binary Search on the Answer", url: "https://usaco.guide/silver/binary-search", source: "USACO Guide" },
    ],
  },

  "sorting": {
    id: "sorting",
    name: "Sorting",
    summary: "Order data to expose structure; the right sort turns hard problems into easy scans.",
    overview:
      "## The core idea\n\nSorting arranges elements by a key. Most languages give you an `O(n log n)` comparison sort; the real skill is **knowing when sorting is the unlock** and choosing the right key. Comparison sorts have a proven `O(n log n)` lower bound; non-comparison sorts (counting, radix, bucket) beat it for bounded integer keys.\n\n## How to think\n\nWhen stuck, ask: *does the answer become obvious if the data were sorted?* Sorting groups equal items together, puts extremes at the ends, and makes neighbors comparable — which is exactly what greedy, two-pointer, sweep-line, and interval problems need. Often the entire solution is **sort by the right key, then one linear pass**. So the design decision is the *comparator/key*, not the algorithm.\n\nKnow your tools: Python's `sorted`/`list.sort` is **Timsort** — stable and `O(n log n)`. Use `key=` for custom order, `reverse=True` for descending, and tuple keys for multi-level sorts. Reach for **counting sort** only when keys are small bounded integers and you need `O(n + k)`.\n\n## Variants\n\n- **Merge sort**: stable, `O(n log n)`, the model for divide-and-conquer and counting inversions.\n- **Quick sort / quickselect**: in-place average `O(n log n)`; quickselect finds the kth element in `O(n)` average.\n- **Counting / radix / bucket**: linear-time for bounded-range keys.\n- **Custom-key & multi-key**: the interview workhorse — sort by a derived key, then scan.\n\n## Edge thinking\n\n**Stability matters** when a secondary order must be preserved (sort by score, keep input order on ties). Quicksort is *not* stable; merge sort and Timsort are. Beware quicksort's `O(n^2)` worst case on adversarial/sorted input — randomize the pivot.",
    recognition: [
      "The problem becomes a simple scan, greedy, or two-pointer once data is ordered",
      "Intervals to merge, schedule, or sweep — sort by start (or end)",
      "Find kth largest/smallest, median, or top-k",
      "Detect duplicates, anagrams, or group equal items",
      "Keys are small bounded integers (ages, digits, letters) → counting/radix",
      "You must preserve original order among equal keys (stability)",
      "'Sort by X, then by Y' multi-criteria ordering",
    ],
    whenToUse: [
      "Preprocessing for two-pointer, greedy, binary search, or sweep-line solutions",
      "Merging or scheduling intervals and meeting-room style problems",
      "Selecting order statistics (kth element) — quickselect or a heap",
      "Linear-time sort when integer keys fit a small known range (counting/radix)",
    ],
    whenNotToUse: [
      "Data is already in a useful order or only needs a single min/max (an O(n) scan beats O(n log n))",
      "You need the streaming top-k from a huge stream (a heap is better than sorting all of it)",
      "Order does not help and a hash map gives O(1) grouping/lookup",
    ],
    patterns: [
      { name: "Sort + linear scan", note: "the workhorse: pick the key, then sweep once" },
      { name: "Merge sort", note: "stable divide-and-conquer; counts inversions" },
      { name: "Quickselect", note: "kth element in O(n) average via partition" },
      { name: "Counting / radix sort", note: "O(n+k) for bounded integer keys, stable" },
      { name: "Custom / multi-key", note: "key= function or tuple keys for tie-breaking" },
    ],
    complexity: [
      { operation: "Comparison sort (Timsort/merge)", time: "O(n log n)", space: "O(n)", note: "stable; lower bound for comparison sorts" },
      { operation: "Quicksort (average)", time: "O(n log n)", space: "O(log n)", note: "in-place; O(n^2) worst case" },
      { operation: "Quickselect (average)", time: "O(n)", space: "O(1)", note: "kth element without full sort" },
      { operation: "Counting / radix sort", time: "O(n + k)", space: "O(n + k)", note: "k = key range; integers only" },
    ],
    edgeCases: [
      "Empty array or single element — already sorted",
      "All equal elements — stability and quicksort pivot behavior",
      "Already sorted or reverse-sorted input — quicksort worst case without randomization",
      "Negative numbers with counting/radix sort (offset the keys)",
      "Sorting tuples/objects where a tie-break key is required",
      "Very large n where O(n) extra space for merge sort matters",
    ],
    commonMistakes: [
      "Assuming the language sort is stable when it is not (or vice versa)",
      "Sorting when an O(n) scan or a heap would do, wasting a log factor",
      "Quicksort on sorted input without pivot randomization → O(n^2) TLE",
      "Counting sort on unbounded or huge key ranges (k dominates)",
      "Comparator that is not a strict weak ordering (inconsistent comparisons crash/loop)",
      "Mutating the list with .sort() when you needed the original order elsewhere",
    ],
    template: {
      language: "python",
      code: "# Idiomatic Python sorting\nnums.sort()                              # in place, ascending, stable (Timsort)\nordered = sorted(nums, reverse=True)     # new list, descending\n\n# custom and multi-level keys\npairs.sort(key=lambda p: (p[0], -p[1]))  # by first asc, then second desc\n\n# merge sort: the canonical O(n log n) divide-and-conquer\ndef merge_sort(a):\n    if len(a) <= 1:\n        return a\n    mid = len(a) // 2\n    left, right = merge_sort(a[:mid]), merge_sort(a[mid:])\n    merged, i, j = [], 0, 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:          # <= keeps it stable\n            merged.append(left[i]); i += 1\n        else:\n            merged.append(right[j]); j += 1\n    merged.extend(left[i:]); merged.extend(right[j:])\n    return merged",
    },
    observations: [
      "Most 'hard' array problems are easy after the right sort + one pass — the insight is the key, not the algorithm",
      "Python's sort is Timsort: stable and near-linear on partially sorted data",
      "Tuple keys give free multi-level sorting; negate a numeric field to flip its direction",
      "Comparison sorts cannot beat O(n log n); counting/radix can because they don't compare",
      "Quickselect gives the kth element in O(n) average — no need to sort everything",
      "Stability lets you sort by secondary key first, then primary, to compose orderings",
    ],
    problems: [
      { title: "Sort Colors", url: "https://leetcode.com/problems/sort-colors/", difficulty: "Medium", pattern: "Dutch national flag / counting sort, 3 buckets" },
      { title: "Merge Intervals", url: "https://leetcode.com/problems/merge-intervals/", difficulty: "Medium", pattern: "sort by start, then linear merge" },
      { title: "Kth Largest Element in an Array", url: "https://leetcode.com/problems/kth-largest-element-in-an-array/", difficulty: "Medium", pattern: "quickselect / heap order statistic" },
      { title: "Sort an Array", url: "https://leetcode.com/problems/sort-an-array/", difficulty: "Medium", pattern: "implement merge sort / quicksort yourself" },
      { title: "Maximum Gap", url: "https://leetcode.com/problems/maximum-gap/", difficulty: "Hard", pattern: "bucket / radix sort for O(n)" },
    ],
    videos: [
      { title: "Sort an Array (Merge Sort) - Leetcode 912 - Python", url: "https://www.youtube.com/watch?v=MsYZSinhuFo", source: "NeetCode" },
      { title: "Sorting Algorithms (full playlist)", url: "https://www.youtube.com/playlist?list=PL99hZ39RJtzXOa1SIgYKGNV-4s9Hi2QxF", source: "Abdul Bari" },
      { title: "Understanding Sorting Algorithms (full course)", url: "https://www.youtube.com/watch?v=l7-f9gS8VOs", source: "freeCodeCamp" },
    ],
    articles: [
      { title: "Sorting Algorithms", url: "https://www.geeksforgeeks.org/sorting-algorithms/", source: "GeeksforGeeks" },
      { title: "Sorting algorithm", url: "https://en.wikipedia.org/wiki/Sorting_algorithm", source: "Wikipedia" },
      { title: "Sorting and Searching", url: "https://usaco.guide/silver/sorting-custom", source: "USACO Guide" },
    ],
  },

  "stack": {
    id: "stack",
    name: "Stack",
    summary: "A LIFO structure for nesting, backtracking, and 'most recent first' processing.",
    overview:
      "## The core idea\n\nA **stack** is last-in-first-out (LIFO): you only touch the top. `push`, `pop`, and `peek` are all `O(1)`. In Python a plain `list` is a stack — `append` to push, `pop()` to pop. Its superpower is modeling **nesting and recency**: the thing you must resolve next is always the most recent unresolved thing.\n\n## How to think\n\nAsk: *does the current element need to be matched against, or undo, the most recent prior element?* If processing has a 'matching pairs', 'innermost first', or 'remember until resolved' flavor, a stack fits. Brackets, expression evaluation, and function-call simulation all share this shape. A second cue: **any recursive idea can be made iterative with an explicit stack** — useful when recursion depth would overflow.\n\nThe canonical bracket-matching mental model: push every opener; on a closer, the top must be its partner or the string is invalid; the stack must be empty at the end.\n\n## Variants\n\n- **Matching / validation**: balanced parentheses, valid nesting.\n- **Expression evaluation**: RPN, infix-to-postfix, calculators.\n- **Min/Max stack**: O(1) current minimum via a paired stack.\n- **Iterative DFS / call-stack simulation**: replace recursion with an explicit stack.\n- **Monotonic stack**: a specialized variant (next-greater style) — see its own concept.\n\n## Edge thinking\n\nAlways guard `pop`/`peek` against an **empty stack** — a closer with nothing to match, or evaluating an operator with too few operands, is the classic crash. For validity problems, an empty stack at the end is usually part of the success condition.",
    recognition: [
      "Matching pairs: parentheses, brackets, tags, quotes",
      "Evaluating or converting expressions (RPN, infix/postfix, calculators)",
      "'Undo', backtracking, or 'most recent first' processing",
      "You need the previous unresolved element while scanning",
      "Converting a recursive DFS to iterative form",
      "Need O(1) access to the running min/max alongside push/pop",
      "Simplifying paths, removing adjacent duplicates, or collapsing nesting",
    ],
    whenToUse: [
      "Validating or processing nested / paired structure",
      "Evaluating expressions or simulating a call stack iteratively",
      "Maintaining an auxiliary min/max with each element (min stack)",
      "Any 'resolve against the most recent item' scan",
    ],
    whenNotToUse: [
      "You need first-in-first-out order (use a queue / deque)",
      "You need random access or indexing into the middle (use an array)",
      "You need the min/max over a sliding range, not all-time (use a monotonic deque)",
    ],
    patterns: [
      { name: "Bracket matching", note: "push openers; closer must match top; empty at end" },
      { name: "Expression evaluation", note: "push operands; operator pops its args, pushes result" },
      { name: "Min / Max stack", note: "second stack tracks running min for O(1) getMin" },
      { name: "Iterative DFS", note: "explicit stack of nodes/states replaces recursion" },
      { name: "Collapse / simplify", note: "pop when current cancels the top (path, dedupe)" },
    ],
    complexity: [
      { operation: "push / pop / peek", time: "O(1)", space: "O(1)", note: "amortized for list-backed stack" },
      { operation: "Scan with stack (e.g. brackets)", time: "O(n)", space: "O(n)", note: "each element pushed/popped once" },
      { operation: "Min stack getMin", time: "O(1)", space: "O(n)", note: "auxiliary stack of minima" },
    ],
    edgeCases: [
      "Empty input — typically valid / trivial result",
      "Closer with empty stack — invalid, must guard before pop",
      "Leftover openers at end — unbalanced, stack not empty",
      "Single element",
      "Operator with too few operands in expression eval",
      "Deeply nested input pushing stack/recursion depth limits",
    ],
    commonMistakes: [
      "Popping or peeking without checking the stack is non-empty",
      "Forgetting to require an empty stack at the end of a validity check",
      "Using a queue when you needed LIFO order (or vice versa)",
      "Mismatched bracket-type check (only counting, not matching the kind)",
      "In expression eval, popping operands in the wrong order for non-commutative ops",
      "Mutating the stack while iterating over it",
    ],
    template: {
      language: "python",
      code: "def is_valid(s):\n    # balanced brackets: push openers, match closers against top\n    pairs = {')': '(', ']': '[', '}': '{'}\n    stack = []\n    for ch in s:\n        if ch in '([{':\n            stack.append(ch)\n        else:\n            # closer: stack must be non-empty and top must match\n            if not stack or stack[-1] != pairs[ch]:\n                return False\n            stack.pop()\n    return not stack            # valid only if nothing is left unmatched\n\ndef eval_rpn(tokens):\n    stack = []\n    ops = {'+': lambda a, b: a + b, '-': lambda a, b: a - b,\n           '*': lambda a, b: a * b, '/': lambda a, b: int(a / b)}\n    for t in tokens:\n        if t in ops:\n            b, a = stack.pop(), stack.pop()   # order matters for - and /\n            stack.append(ops[t](a, b))\n        else:\n            stack.append(int(t))\n    return stack[-1]",
    },
    observations: [
      "A Python list is already a stack: append = push, pop() = pop, [-1] = peek",
      "Empty-stack-at-the-end is the success signal for most matching problems",
      "Any recursion can be rewritten with an explicit stack to dodge depth limits",
      "A min stack stores (value, current_min) pairs or a parallel minima stack for O(1) getMin",
      "Order of popping operands matters for non-commutative operators",
      "If you find yourself needing the most-recent-larger element, you want a monotonic stack",
    ],
    problems: [
      { title: "Valid Parentheses", url: "https://leetcode.com/problems/valid-parentheses/", difficulty: "Easy", pattern: "bracket matching, empty-at-end" },
      { title: "Min Stack", url: "https://leetcode.com/problems/min-stack/", difficulty: "Medium", pattern: "auxiliary stack for O(1) getMin" },
      { title: "Evaluate Reverse Polish Notation", url: "https://leetcode.com/problems/evaluate-reverse-polish-notation/", difficulty: "Medium", pattern: "operand stack expression evaluation" },
      { title: "Generate Parentheses", url: "https://leetcode.com/problems/generate-parentheses/", difficulty: "Medium", pattern: "stack-driven backtracking" },
      { title: "Basic Calculator", url: "https://leetcode.com/problems/basic-calculator/", difficulty: "Hard", pattern: "stack for nested parentheses and signs" },
    ],
    videos: [
      { title: "Valid Parentheses - Stack - Leetcode 20 - Python", url: "https://www.youtube.com/watch?v=WTzjTskDFMg", source: "NeetCode" },
      { title: "Stack Problems (full playlist)", url: "https://www.youtube.com/playlist?list=PLot-Xpze53lfxD6l5pAGvCD4nPvWKU8Qo", source: "NeetCode" },
      { title: "Data Structures: Stack (intro + implementation)", url: "https://www.youtube.com/watch?v=RBSGKlAvoiM", source: "William Fiset / freeCodeCamp" },
    ],
    articles: [
      { title: "Stack Data Structure", url: "https://www.geeksforgeeks.org/stack-data-structure/", source: "GeeksforGeeks" },
      { title: "Stack (abstract data type)", url: "https://en.wikipedia.org/wiki/Stack_(abstract_data_type)", source: "Wikipedia" },
      { title: "Stacks (programming guide)", url: "https://usaco.guide/silver/stacks", source: "USACO Guide" },
    ],
  },

  "monotonic-stack": {
    id: "monotonic-stack",
    name: "Monotonic Stack",
    summary: "A stack kept sorted so each element finds its next/previous greater-or-smaller in O(n).",
    overview:
      "## The core idea\n\nA **monotonic stack** is an ordinary stack you keep strictly increasing or decreasing by popping anything that breaks the order before pushing. That single discipline answers **next/previous greater/smaller element** questions for every position in one `O(n)` pass — each index is pushed and popped at most once.\n\n## How to think\n\nWhen you catch yourself wanting, for each element, *'the nearest element to the left/right that is bigger/smaller than me'*, that is the monotonic-stack signal. The pattern: as you scan, **while the new element violates the stack's monotonic property, pop — and the popped element has just found its answer** (the new element is its next-greater/smaller). The stack holds 'candidates still waiting for their answer'.\n\nThe two design choices are: **(1) increasing vs decreasing** (decreasing stack ⇒ next *greater* element; increasing ⇒ next *smaller*), and **(2) store indices, not values**, so you can compute distances and spans. Direction matters: scan left→right for 'next', right→left for 'previous'.\n\n## Variants\n\n- **Next greater / smaller element** (and previous).\n- **Spans & distances**: stock span, days until warmer.\n- **Histogram / rectangle**: largest rectangle, maximal area — each bar's left/right smaller bound.\n- **Circular array**: iterate `2n` with modulo for next-greater in a ring.\n\n## Edge thinking\n\nElements **left on the stack** at the end have *no* next-greater/smaller — initialize their answer (e.g. `-1` or `0`). Decide `>` vs `>=` carefully: it controls how **equal values** are treated and whether you get the strictly-greater or first-not-smaller neighbor.",
    recognition: [
      "For each element you need the nearest greater/smaller element to one side",
      "'Next greater element', 'previous smaller element', or warmer/cooler day",
      "Span problems: stock span, how many days until a larger value",
      "Largest rectangle in a histogram / maximal rectangle in a matrix",
      "A brute force is O(n^2) double loop comparing each element to others one side",
      "Trapping rain water or skyline-style boundary problems",
      "Circular 'next greater' where the array wraps around",
    ],
    whenToUse: [
      "Computing next/previous greater or smaller for every index in O(n)",
      "Span, distance, or 'how long until a bigger value' problems",
      "Histogram area / rectangle problems needing left and right smaller bounds",
      "Replacing an O(n^2) nearest-larger/smaller scan with one pass",
    ],
    whenNotToUse: [
      "You need a global max/min, not the nearest neighbor (a simple scan suffices)",
      "The comparison is not 'nearest greater/smaller' but something order-insensitive",
      "You need the max over a sliding window of fixed size (use a monotonic deque/queue instead)",
    ],
    patterns: [
      { name: "Next greater element", note: "decreasing stack of indices; pop while cur > top, scan L->R" },
      { name: "Previous smaller element", note: "increasing stack; scan and read top before pushing" },
      { name: "Histogram largest rectangle", note: "increasing stack; width on pop = i - stack[-1] - 1" },
      { name: "Span / distance", note: "store indices; answer = i - popped_index" },
      { name: "Circular next greater", note: "iterate 2n with i % n to wrap the ring" },
    ],
    complexity: [
      { operation: "Single pass, all next-greater", time: "O(n)", space: "O(n)", note: "each index pushed/popped once" },
      { operation: "Largest rectangle in histogram", time: "O(n)", space: "O(n)", note: "amortized O(1) per bar" },
      { operation: "Circular next greater", time: "O(n)", space: "O(n)", note: "iterate 2n indices" },
    ],
    edgeCases: [
      "Elements with no greater/smaller neighbor — default answer (-1 / 0)",
      "All increasing input — nothing pops until the end (or every push, depending on direction)",
      "All decreasing input — each element pops the previous immediately",
      "Equal adjacent values — choice of > vs >= decides their handling",
      "Empty array or single element",
      "Histogram: implicit sentinel of height 0 to flush the stack at the end",
    ],
    commonMistakes: [
      "Storing values instead of indices, then being unable to compute distances/spans",
      "Wrong monotonic direction (increasing vs decreasing) for the asked relation",
      "Using >= when you needed > (or vice versa) for equal elements",
      "Forgetting to assign defaults to indices left on the stack at the end",
      "Reading the answer after pushing instead of before (previous-element variants)",
      "In histogram problems, omitting the trailing sentinel so the stack never flushes",
    ],
    template: {
      language: "python",
      code: "def next_greater(nums):\n    # for each i, the next element to the right that is strictly greater (else -1)\n    n = len(nums)\n    ans = [-1] * n\n    stack = []                       # holds indices, values DECREASING\n    for i, x in enumerate(nums):\n        # x resolves everything smaller than it that is still waiting\n        while stack and nums[stack[-1]] < x:\n            ans[stack.pop()] = x\n        stack.append(i)\n    return ans                       # indices left on stack keep -1\n\ndef largest_rectangle(heights):\n    stack = []                       # indices, heights INCREASING\n    best = 0\n    for i, h in enumerate(heights + [0]):   # sentinel flushes the stack\n        while stack and heights[stack[-1]] > h:\n            height = heights[stack.pop()]\n            left = stack[-1] if stack else -1\n            best = max(best, height * (i - left - 1))\n        stack.append(i)\n    return best",
    },
    observations: [
      "Each index is pushed and popped at most once, so the whole scan is O(n) despite the inner while",
      "Decreasing stack answers 'next greater'; increasing stack answers 'next smaller'",
      "Store indices, not values — you almost always need positions for spans and widths",
      "The popped element is the one that just found its answer, not the element you pushed",
      "A height-0 sentinel at the end of a histogram guarantees the stack fully flushes",
      "Whatever remains on the stack had no qualifying neighbor — that is its default answer",
    ],
    problems: [
      { title: "Next Greater Element I", url: "https://leetcode.com/problems/next-greater-element-i/", difficulty: "Easy", pattern: "next greater via decreasing stack + map" },
      { title: "Daily Temperatures", url: "https://leetcode.com/problems/daily-temperatures/", difficulty: "Medium", pattern: "distance to next warmer day, store indices" },
      { title: "Next Greater Element II", url: "https://leetcode.com/problems/next-greater-element-ii/", difficulty: "Medium", pattern: "circular next greater, iterate 2n" },
      { title: "Online Stock Span", url: "https://leetcode.com/problems/online-stock-span/", difficulty: "Medium", pattern: "previous-greater span, accumulate widths" },
      { title: "Largest Rectangle in Histogram", url: "https://leetcode.com/problems/largest-rectangle-in-histogram/", difficulty: "Hard", pattern: "increasing stack, width on pop, sentinel" },
    ],
    videos: [
      { title: "Daily Temperatures - Monotonic Stack - Leetcode 739 - Python", url: "https://www.youtube.com/watch?v=cTBiBSnjO3c", source: "NeetCode" },
      { title: "Stack Problems (Monotonic Stack playlist)", url: "https://www.youtube.com/playlist?list=PLot-Xpze53lfxD6l5pAGvCD4nPvWKU8Qo", source: "NeetCode" },
      { title: "Introduction to Monotonic Stacks (Daily Temperatures)", url: "https://www.youtube.com/watch?v=DmHEl0v_hO0", source: "Geekific" },
    ],
    articles: [
      { title: "Introduction to Monotonic Stack", url: "https://www.geeksforgeeks.org/introduction-to-monotonic-stack-2/", source: "GeeksforGeeks" },
      { title: "Next Greater Element", url: "https://www.geeksforgeeks.org/next-greater-element/", source: "GeeksforGeeks" },
      { title: "Stacks (covers monotonic stack)", url: "https://usaco.guide/gold/stacks", source: "USACO Guide" },
    ],
  },

  "queue": {
    id: "queue",
    name: "Queue",
    summary: "A FIFO structure for order-preserving processing, BFS, and sliding-window fronts.",
    overview:
      "## The core idea\n\nA **queue** is first-in-first-out (FIFO): you add at the back and remove from the front, both `O(1)`. In Python, **use `collections.deque`** (`append` / `popleft`) — a plain list's `pop(0)` is `O(n)`. The queue's job is to process things **in arrival order**, which makes it the backbone of breadth-first search and streaming windows.\n\n## How to think\n\nAsk: *must items be handled in the order they arrived, expanding outward level by level?* If yes — shortest path in an unweighted graph, level-order traversal, task scheduling — a queue is the structure. The BFS mental model: the queue holds the **frontier** of nodes discovered but not yet expanded; you pop one, push its undiscovered neighbors, and the FIFO order guarantees you reach nearer nodes first, so the first time you see a node is via a shortest path.\n\nA **deque** generalizes the queue (push/pop both ends). The headline use is the **monotonic deque** for sliding-window max/min in `O(n)`: keep the window's candidates in decreasing order, drop indices that fall out of the window from the front, and pop weaker candidates from the back.\n\n## Variants\n\n- **Plain FIFO queue**: ordered processing, scheduling.\n- **BFS queue**: level-order / shortest path in unweighted graphs.\n- **Deque (double-ended)**: push/pop both ends; sliding-window max/min.\n- **Circular queue / ring buffer**: fixed capacity with wraparound indices.\n- **Queue from two stacks**: amortized O(1) FIFO using LIFO building blocks.\n\n## Edge thinking\n\nGuard `popleft` against an **empty queue**. In BFS, **mark nodes visited when you enqueue them, not when you dequeue** — otherwise the same node is added many times and the queue blows up. For sliding-window deques, store **indices** so you can tell when the front has slid out of range.",
    recognition: [
      "Process items strictly in arrival order (FIFO)",
      "Shortest path or level-order traversal in an unweighted graph/tree (BFS)",
      "Sliding-window maximum or minimum over a stream",
      "Task / request scheduling, rate limiting, recent-calls within a time window",
      "Producer/consumer or buffering between stages",
      "'Most recent N within a window' counting problems",
      "Multi-source spread/flood (rotting oranges, fire spread) — BFS from many starts",
    ],
    whenToUse: [
      "Breadth-first search for shortest paths in unweighted graphs",
      "Level-order tree traversal and multi-source BFS",
      "Sliding-window max/min and bounded-window aggregates (monotonic deque)",
      "Ordered scheduling, buffering, or windowed event counting",
    ],
    whenNotToUse: [
      "You need last-in-first-out / backtracking order (use a stack)",
      "Edges have weights — BFS no longer gives shortest paths (use Dijkstra)",
      "You need random access or sorting of the elements (use an array/heap)",
    ],
    patterns: [
      { name: "BFS frontier", note: "deque of frontier nodes; mark visited on enqueue" },
      { name: "Level-order", note: "process len(queue) nodes per level snapshot" },
      { name: "Monotonic deque", note: "decreasing indices for sliding-window max in O(n)" },
      { name: "Circular queue", note: "fixed array + head/tail mod capacity (ring buffer)" },
      { name: "Queue via two stacks", note: "in-stack + out-stack; refill out when empty, amortized O(1)" },
    ],
    complexity: [
      { operation: "enqueue / dequeue (deque)", time: "O(1)", space: "O(1)", note: "list.pop(0) is O(n) — use deque" },
      { operation: "BFS over graph", time: "O(V + E)", space: "O(V)", note: "each node/edge visited once" },
      { operation: "Sliding-window max (deque)", time: "O(n)", space: "O(k)", note: "each index pushed/popped once" },
      { operation: "Queue via two stacks", time: "O(1) amortized", space: "O(n)", note: "each element moved at most twice" },
    ],
    edgeCases: [
      "Empty queue — guard popleft before accessing",
      "Single node / single element",
      "BFS on a disconnected graph — may need to start from multiple sources",
      "Sliding window of size 1, or window larger than the array",
      "Cycles in the graph — visited set prevents infinite enqueue",
      "Circular queue full vs empty disambiguation (size counter or one empty slot)",
    ],
    commonMistakes: [
      "Using list.pop(0) for dequeue — O(n) each, turns BFS into O(n^2)",
      "Marking visited on dequeue instead of enqueue, re-adding nodes many times",
      "Storing values instead of indices in a sliding-window deque, losing the window bound",
      "Forgetting to bound the window: not popping the front when it slides out of range",
      "Confusing FIFO (queue) with LIFO (stack) for the required order",
      "Using BFS for shortest path on a weighted graph",
    ],
    template: {
      language: "python",
      code: "from collections import deque\n\ndef bfs_shortest(graph, start):\n    # shortest distance (in edges) from start in an unweighted graph\n    dist = {start: 0}\n    q = deque([start])\n    while q:\n        node = q.popleft()\n        for nxt in graph[node]:\n            if nxt not in dist:          # mark visited on ENQUEUE\n                dist[nxt] = dist[node] + 1\n                q.append(nxt)\n    return dist\n\ndef max_sliding_window(nums, k):\n    # monotonic deque of indices, values DECREASING; front = window max\n    dq, out = deque(), []\n    for i, x in enumerate(nums):\n        while dq and nums[dq[-1]] <= x:  # drop weaker candidates\n            dq.pop()\n        dq.append(i)\n        if dq[0] <= i - k:               # front slid out of the window\n            dq.popleft()\n        if i >= k - 1:\n            out.append(nums[dq[0]])\n    return out",
    },
    observations: [
      "Always use collections.deque for queues; list.pop(0) is O(n) and silently degrades BFS",
      "BFS gives shortest paths only because FIFO order expands the nearest frontier first",
      "Mark visited at enqueue time — the single most common BFS bug otherwise",
      "Process len(queue) items per iteration to get clean level-by-level BFS",
      "A monotonic deque stores indices so the front can be evicted when it leaves the window",
      "Two stacks make an amortized-O(1) queue: pour into the out-stack only when it empties",
    ],
    problems: [
      { title: "Number of Recent Calls", url: "https://leetcode.com/problems/number-of-recent-calls/", difficulty: "Easy", pattern: "FIFO window, evict pings older than t-3000" },
      { title: "Implement Queue using Stacks", url: "https://leetcode.com/problems/implement-queue-using-stacks/", difficulty: "Easy", pattern: "two-stack queue, amortized O(1)" },
      { title: "Design Circular Queue", url: "https://leetcode.com/problems/design-circular-queue/", difficulty: "Medium", pattern: "ring buffer with head/tail wraparound" },
      { title: "Rotting Oranges", url: "https://leetcode.com/problems/rotting-oranges/", difficulty: "Medium", pattern: "multi-source BFS, level = minute" },
      { title: "Sliding Window Maximum", url: "https://leetcode.com/problems/sliding-window-maximum/", difficulty: "Hard", pattern: "monotonic deque, O(n) window max" },
    ],
    videos: [
      { title: "Implement Queue using Stacks - Leetcode 232 - Python", url: "https://www.youtube.com/watch?v=eanwa3ht3YQ", source: "NeetCode" },
      { title: "Sliding Window Maximum - Monotonic Queue - Leetcode 239", url: "https://www.youtube.com/watch?v=DfljaUwZsOk", source: "NeetCode" },
      { title: "Data Structures: Queue (intro + implementation)", url: "https://www.youtube.com/watch?v=RBSGKlAvoiM", source: "William Fiset / freeCodeCamp" },
    ],
    articles: [
      { title: "Queue Data Structure", url: "https://www.geeksforgeeks.org/queue-data-structure/", source: "GeeksforGeeks" },
      { title: "Queue (abstract data type)", url: "https://en.wikipedia.org/wiki/Queue_(abstract_data_type)", source: "Wikipedia" },
      { title: "Breadth First Search (BFS)", url: "https://cp-algorithms.com/graph/breadth-first-search.html", source: "CP-Algorithms" },
    ],
  },
};
