// Concepts: BFS · Topological Sort · Union Find (Disjoint Set) · Shortest Path · Minimum Spanning Tree · Greedy
export const BATCH = {
  "bfs": {
    id: "bfs",
    name: "BFS",
    summary: "Explore a graph level by level with a queue; the canonical tool for shortest paths in unweighted graphs.",
    overview:
      "## The core idea\n\nBreadth-First Search visits nodes in **waves**: all nodes at distance 0, then distance 1, then 2, and so on. A FIFO **queue** holds the frontier and a **visited** set stops re-processing. Because nodes come off the queue in non-decreasing distance order, **the first time you reach a node is along a shortest path** in an unweighted graph.\n\n## How to think\n\nAsk three questions: **What is a node?** (a cell, a string, a state). **What is an edge?** (the legal moves from that node). **When do I mark visited?** Mark a node visited *when you enqueue it*, not when you dequeue it — otherwise duplicates pile up in the queue and you revisit. To track distance, either store `(node, dist)` in the queue or process the queue **one full level at a time** with `for _ in range(len(queue))`.\n\n## Variants\n\n- **Grid BFS** — neighbors are the 4 (or 8) adjacent cells; bound-check before enqueue.\n- **Multi-source BFS** — seed the queue with *all* sources at once (e.g. rotting oranges, 0/1 matrix); every cell gets its distance to the nearest source in one pass.\n- **0-1 BFS** — edges weighted 0 or 1; use a deque, push 0-edges to the front.\n- **Word/state BFS** — nodes are abstract states; generate neighbors on the fly.\n\n## Edge thinking\n\nThe classic bug is marking visited on dequeue; the classic crash is forgetting that the start node must be enqueued and marked before the loop. Disconnected graphs need a loop over all start nodes.",
    recognition: [
      "The question asks for the shortest path / fewest steps / minimum moves in an unweighted graph or grid",
      "You need the minimum number of transformations between states (word ladder, gene mutations)",
      "A grid spreads or floods uniformly from one or many sources (infection, water, fire)",
      "You must find the nearest cell/node with some property to every other cell",
      "Level-order traversal of a tree or graph is requested explicitly",
      "All edges have equal cost (or no cost) and you want the closest reachable thing",
    ],
    whenToUse: [
      "Shortest path or minimum steps when every edge has the same weight",
      "Multi-source spread problems where many cells start active simultaneously",
      "Level-by-level processing where the answer depends on the wave number",
      "Connectivity / reachability when you also care about distance, not just yes/no",
    ],
    whenNotToUse: [
      "Edges have differing positive weights — use Dijkstra instead",
      "You only need to know if a path exists or to enumerate components — DFS is simpler and uses less memory on deep graphs",
      "The state space is huge and you need just one path quickly — A* or bidirectional search beats plain BFS",
    ],
    patterns: [
      { name: "Level-order BFS", note: "snapshot len(queue) per level so you process exactly one wave before incrementing distance" },
      { name: "Multi-source BFS", note: "enqueue every source first; one sweep gives nearest-source distance to all cells" },
      { name: "Grid BFS", note: "directions = [(0,1),(0,-1),(1,0),(-1,0)]; bound-check and visited-check before enqueue" },
      { name: "State-space BFS", note: "nodes are abstract states (strings, tuples); generate neighbors lazily, use a set for visited" },
    ],
    complexity: [
      { operation: "BFS over graph", time: "O(V + E)", space: "O(V)", note: "each vertex and edge processed once; queue + visited hold O(V)" },
      { operation: "BFS over m×n grid", time: "O(m*n)", space: "O(m*n)", note: "each cell enqueued at most once" },
      { operation: "Word/state BFS", time: "O(N * L * 26)", space: "O(N * L)", note: "N words, length L; neighbor generation dominates" },
    ],
    edgeCases: [
      "Empty graph / empty grid — return early",
      "Start equals target — distance is 0, handle before the loop",
      "Disconnected graph — wrap BFS in a loop over all unvisited nodes",
      "Single node with no edges",
      "All cells already at target state (multi-source) — answer is 0",
      "Grid fully blocked — some cells unreachable, return -1 or leave at infinity",
    ],
    commonMistakes: [
      "Marking visited on dequeue instead of on enqueue — causes the same node to be queued many times",
      "Forgetting to mark/enqueue the start node before the main loop",
      "Mixing up row/column indices or directions when bound-checking a grid",
      "Tracking distance with a separate counter instead of per-level snapshots, so the count drifts",
      "Using a list with pop(0) instead of collections.deque — pop(0) is O(n), turning BFS into O(n^2)",
      "Not handling unreachable nodes, returning a stale distance instead of -1",
    ],
    template: {
      language: "python",
      code: "from collections import deque\n\ndef bfs_shortest(grid, start):\n    rows, cols = len(grid), len(grid[0])\n    q = deque([start])\n    visited = {start}\n    dist = 0\n    dirs = [(0, 1), (0, -1), (1, 0), (-1, 0)]\n    while q:\n        for _ in range(len(q)):          # process one level\n            r, c = q.popleft()\n            # if (r, c) is the target: return dist\n            for dr, dc in dirs:\n                nr, nc = r + dr, c + dc\n                if (0 <= nr < rows and 0 <= nc < cols\n                        and (nr, nc) not in visited\n                        and grid[nr][nc] != 1):   # 1 = wall\n                    visited.add((nr, nc))         # mark on enqueue\n                    q.append((nr, nc))\n        dist += 1\n    return -1",
    },
    observations: [
      "The first time BFS reaches a node, that distance is optimal — no later path can be shorter in an unweighted graph",
      "Multi-source BFS is just BFS seeded with many starts; it costs the same O(V+E) as single-source",
      "BFS uses more memory than DFS because the queue can hold an entire level — wide graphs are expensive",
      "0-1 BFS with a deque gives Dijkstra-quality answers in O(V+E) when weights are only 0 or 1",
      "Encoding state as a hashable tuple lets BFS explore puzzles and games without an explicit graph",
    ],
    problems: [
      { title: "Binary Tree Level Order Traversal", url: "https://leetcode.com/problems/binary-tree-level-order-traversal/", difficulty: "Medium", pattern: "level-order BFS, snapshot queue length per level" },
      { title: "Number of Islands", url: "https://leetcode.com/problems/number-of-islands/", difficulty: "Medium", pattern: "grid BFS flood fill over connected components" },
      { title: "Rotting Oranges", url: "https://leetcode.com/problems/rotting-oranges/", difficulty: "Medium", pattern: "multi-source BFS, count waves until done" },
      { title: "01 Matrix", url: "https://leetcode.com/problems/01-matrix/", difficulty: "Medium", pattern: "multi-source BFS from all zeros outward" },
      { title: "Word Ladder", url: "https://leetcode.com/problems/word-ladder/", difficulty: "Hard", pattern: "state-space BFS over word transformations" },
    ],
    videos: [
      { title: "G-5. Breadth-First Search (BFS) | C++ and Java | Traversal Technique in Graphs", url: "https://www.youtube.com/watch?v=-tgVpUgsQ5k", source: "takeUforward (Striver)" },
      { title: "Breadth-First Search (BFS) in Graph: A Step-by-Step Tutorial", url: "https://www.youtube.com/watch?v=rTk9dewULy8", source: "YouTube" },
      { title: "Graph Problems (BFS/DFS playlist)", url: "https://www.youtube.com/playlist?list=PLot-Xpze53ldBT_7QA8NVot219jFNr_GI", source: "NeetCode" },
    ],
    articles: [
      { title: "Breadth First Search or BFS for a Graph", url: "https://www.geeksforgeeks.org/dsa/breadth-first-search-or-bfs-for-a-graph/", source: "GeeksforGeeks" },
      { title: "Breadth-first search", url: "https://en.wikipedia.org/wiki/Breadth-first_search", source: "Wikipedia" },
      { title: "Breadth First Search", url: "https://cp-algorithms.com/graph/breadth-first-search.html", source: "CP-Algorithms" },
    ],
  },

  "topological-sort": {
    id: "topological-sort",
    name: "Topological Sort",
    summary: "A linear ordering of a DAG's nodes so every edge points forward; the standard way to schedule things with dependencies.",
    overview:
      "## The core idea\n\nGiven a **directed acyclic graph** (DAG), a topological sort lists every node so that for each edge `u -> v`, `u` appears before `v`. Intuition: `u` must happen before `v`, so produce all prerequisites before their dependents. A valid ordering exists **iff the graph has no cycle**.\n\n## How to think\n\nFirst decide the **edge direction**: draw it from prerequisite to dependent (`prereq -> course`). Then pick one of two engines. **Kahn's algorithm (BFS)**: compute every node's **in-degree**, start a queue with all in-degree-0 nodes, and repeatedly pop one, append it to the order, and decrement its neighbors' in-degrees, enqueuing any that hit 0. **DFS**: visit deeply, then push a node onto a stack *after* all its descendants are done; the reversed stack is the order.\n\n## Variants\n\n- **Kahn / BFS** — natural cycle detection: if you output fewer than `n` nodes, a cycle exists.\n- **DFS post-order** — track three colors (unvisited / in-stack / done) to spot a back edge = cycle.\n- **Lexicographically smallest order** — replace Kahn's queue with a min-heap.\n- **Layered / parallel scheduling** — process Kahn's queue level by level to get the minimum number of rounds.\n\n## Edge thinking\n\nThe make-or-break case is the **cycle**: always count processed nodes (Kahn) or detect a back edge (DFS) and return empty/false. Self-loops are cycles too, and the empty graph trivially sorts.",
    recognition: [
      "Tasks/courses/jobs have prerequisites and you must find a valid order to do them",
      "The input is a set of 'X must come before Y' constraints",
      "You must detect whether a dependency graph has a cycle (deadlock / impossible schedule)",
      "Build systems, compilation order, or package install order is described",
      "You need to derive an ordering of symbols from ordered comparisons (alien dictionary)",
      "The graph is directed and the question asks for a linear sequence respecting edges",
    ],
    whenToUse: [
      "Scheduling tasks under precedence constraints in a DAG",
      "Detecting cycles in a directed graph as a side effect of ordering",
      "Resolving dependency chains (modules, courses, recipes)",
      "Computing the lexicographically smallest valid order (Kahn + heap)",
    ],
    whenNotToUse: [
      "The graph is undirected — topological order is undefined there",
      "The graph has cycles by nature and you need an order anyway — no valid topo sort exists",
      "You only need any traversal, not an ordering that respects edge direction",
    ],
    patterns: [
      { name: "Kahn's algorithm (BFS)", note: "in-degree queue; pop in-degree-0 nodes, decrement neighbors; count < n means cycle" },
      { name: "DFS post-order", note: "push node after recursing into all children; reverse the finish order" },
      { name: "Cycle detection via colors", note: "white/gray/black states; encountering a gray node = back edge = cycle" },
      { name: "Lexicographic topo sort", note: "use a min-heap instead of a plain queue in Kahn's to break ties smallest-first" },
    ],
    complexity: [
      { operation: "Kahn's algorithm", time: "O(V + E)", space: "O(V + E)", note: "build in-degrees + adjacency, process each edge once" },
      { operation: "DFS topo sort", time: "O(V + E)", space: "O(V)", note: "recursion stack plus visited/order arrays" },
      { operation: "Lexicographic (heap)", time: "O(V log V + E)", space: "O(V + E)", note: "heap operations add the log factor" },
    ],
    edgeCases: [
      "Graph contains a cycle — no valid ordering; return empty list / false",
      "Self-loop on a node — counts as a cycle",
      "Disconnected DAG — every component must still be ordered",
      "Empty graph or single node — trivial valid order",
      "Multiple valid orderings exist — any one is correct unless lexicographic is required",
      "Duplicate edges — handle so in-degree is not double-counted incorrectly",
    ],
    commonMistakes: [
      "Reversing edge direction (dependent -> prerequisite) so the order comes out backwards",
      "Forgetting to detect cycles, returning a partial/garbage order for a cyclic graph",
      "In Kahn's, decrementing in-degree but forgetting to enqueue when it reaches 0",
      "In DFS, appending the node before recursing instead of after, breaking post-order",
      "Not initializing in-degree entries for nodes with no incoming edges",
      "Confusing the gray (in-progress) and black (done) states, so real cycles are missed",
    ],
    template: {
      language: "python",
      code: "from collections import deque\n\ndef topo_sort(n, edges):\n    # edges: list of (u, v) meaning u must come before v\n    adj = [[] for _ in range(n)]\n    indeg = [0] * n\n    for u, v in edges:\n        adj[u].append(v)\n        indeg[v] += 1\n    q = deque(i for i in range(n) if indeg[i] == 0)\n    order = []\n    while q:\n        u = q.popleft()\n        order.append(u)\n        for v in adj[u]:\n            indeg[v] -= 1\n            if indeg[v] == 0:\n                q.append(v)\n    if len(order) != n:\n        return []          # cycle: no valid topological order\n    return order",
    },
    observations: [
      "A topological order exists if and only if the directed graph is acyclic",
      "Kahn's algorithm gives cycle detection for free: if fewer than n nodes are output, a cycle exists",
      "Most graphs have many valid topo orders; uniqueness happens only when a Hamiltonian path of edges exists",
      "Swapping Kahn's queue for a min-heap yields the lexicographically smallest order at an extra log factor",
      "Processing Kahn's queue level by level tells you the minimum number of parallel rounds to finish all tasks",
    ],
    problems: [
      { title: "Find Eventual Safe States", url: "https://leetcode.com/problems/find-eventual-safe-states/", difficulty: "Medium", pattern: "reverse-graph topo sort / cycle detection" },
      { title: "Course Schedule", url: "https://leetcode.com/problems/course-schedule/", difficulty: "Medium", pattern: "cycle detection via Kahn's / DFS" },
      { title: "Course Schedule II", url: "https://leetcode.com/problems/course-schedule-ii/", difficulty: "Medium", pattern: "produce a valid ordering with Kahn's algorithm" },
      { title: "Minimum Height Trees", url: "https://leetcode.com/problems/minimum-height-trees/", difficulty: "Medium", pattern: "peel leaves layer by layer (topo-style trimming)" },
      { title: "Alien Dictionary", url: "https://leetcode.com/problems/alien-dictionary/", difficulty: "Hard", pattern: "build edges from word comparisons, then topo sort" },
    ],
    videos: [
      { title: "Topological Sort | Kahn's Algorithm | Graph Theory", url: "https://www.youtube.com/watch?v=cIBFEhD77b4", source: "WilliamFiset" },
      { title: "G-22. Kahn's Algorithm | Topological Sort Algorithm | BFS", url: "https://www.youtube.com/watch?v=73sneFXuTEg", source: "takeUforward (Striver)" },
      { title: "G-21. Topological Sort Algorithm | DFS", url: "https://www.youtube.com/watch?v=5lZ0iJMrUMk", source: "takeUforward (Striver)" },
    ],
    articles: [
      { title: "Topological Sorting", url: "https://www.geeksforgeeks.org/dsa/topological-sorting/", source: "GeeksforGeeks" },
      { title: "Topological Sorting", url: "https://cp-algorithms.com/graph/topological-sort.html", source: "CP-Algorithms" },
      { title: "Topological sorting", url: "https://en.wikipedia.org/wiki/Topological_sorting", source: "Wikipedia" },
    ],
  },

  "union-find": {
    id: "union-find",
    name: "Union Find (Disjoint Set)",
    summary: "A near-constant-time structure for grouping elements and answering 'are these two in the same set?'",
    overview:
      "## The core idea\n\nUnion-Find (a.k.a. Disjoint Set Union, DSU) maintains a collection of **disjoint sets** under two operations: `find(x)` returns a canonical **representative** of x's set, and `union(a, b)` merges the two sets. Each set is a tree; the root is the representative. Two elements are connected exactly when they share a root.\n\n## How to think\n\nPicture a forest of pointers where each node points at its parent and roots point at themselves. To test connectivity, climb to both roots and compare. To merge, hang one root under the other. Two optimizations make it almost O(1): **path compression** (during `find`, repoint nodes directly at the root) and **union by rank/size** (always attach the smaller tree under the larger). Together they give an inverse-Ackermann amortized cost — effectively constant.\n\n## Variants\n\n- **Union by size** — attach the smaller set under the larger; also lets you query component size.\n- **Union by rank** — attach by approximate height.\n- **Path compression** — flatten the tree on every find.\n- **DSU with extra data** — store edge counts, weights, or parity (weighted DSU) alongside the parent array.\n\n## Edge thinking\n\nThe key insight: DSU answers connectivity *incrementally and offline* — it cannot easily delete edges. If a problem removes connections over time, process events in **reverse**. Always initialize every node as its own parent; a forgotten init is the usual bug.",
    recognition: [
      "You must repeatedly merge groups and ask whether two elements are connected",
      "Counting the number of connected components as edges are added",
      "Detecting whether adding an edge creates a cycle in an undirected graph",
      "Kruskal's MST needs cycle-free edge selection",
      "Grouping equivalent items (accounts, emails, equations) by transitive relation",
      "Dynamic connectivity where unions only add edges, never remove them",
    ],
    whenToUse: [
      "Incremental connectivity queries as edges arrive one by one",
      "Counting or tracking connected components efficiently",
      "Cycle detection in an undirected graph while building it",
      "Kruskal's algorithm to skip edges that would close a cycle",
    ],
    whenNotToUse: [
      "Edges are removed over time — DSU has no efficient delete (process offline in reverse if possible)",
      "You need the actual path between two nodes, not just whether one exists — use BFS/DFS",
      "The graph is directed and you care about reachability direction — DSU only models undirected connectivity",
    ],
    patterns: [
      { name: "Path compression", note: "in find(), repoint each node directly to the root so future finds are flat" },
      { name: "Union by size/rank", note: "hang the smaller/shorter tree under the larger to keep trees shallow" },
      { name: "Component counting", note: "start count = n, decrement on every successful (distinct-root) union" },
      { name: "Weighted / parity DSU", note: "store offset to parent to answer relative-value or bipartite questions" },
    ],
    complexity: [
      { operation: "find / union (optimized)", time: "O(α(n))", space: "O(n)", note: "inverse Ackermann, effectively constant amortized" },
      { operation: "m operations total", time: "O(m α(n))", space: "O(n)", note: "with both path compression and union by rank" },
      { operation: "naive (no optimization)", time: "O(n)", space: "O(n)", note: "tree can degenerate to a chain" },
    ],
    edgeCases: [
      "Single element — its own root, set size 1",
      "Union of two elements already in the same set — no-op, do not change component count",
      "Self-loop edge (u, u) — should not change anything",
      "All elements isolated — n separate components",
      "Repeated union of the same pair — must stay idempotent",
      "Querying an element never explicitly added — ensure parent array covers it",
    ],
    commonMistakes: [
      "Forgetting to initialize parent[i] = i for every element",
      "Decrementing the component count on a union that joins two already-connected nodes",
      "Comparing parent[a] == parent[b] (immediate parents) instead of find(a) == find(b) (roots)",
      "Skipping path compression and union by rank, then timing out on large inputs",
      "Mutating ranks/sizes when both roots are already equal",
      "Using DSU for a problem that deletes edges, where it silently gives wrong answers",
    ],
    template: {
      language: "python",
      code: "class DSU:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.size = [1] * n\n        self.count = n          # number of disjoint sets\n\n    def find(self, x):\n        while self.parent[x] != x:\n            self.parent[x] = self.parent[self.parent[x]]  # path compression\n            x = self.parent[x]\n        return x\n\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra == rb:\n            return False        # already connected\n        if self.size[ra] < self.size[rb]:\n            ra, rb = rb, ra\n        self.parent[rb] = ra    # union by size\n        self.size[ra] += self.size[rb]\n        self.count -= 1\n        return True",
    },
    observations: [
      "With path compression + union by rank, the amortized cost is O(α(n)) — below 5 for any realistic n",
      "union() returning False (already connected) is exactly the cycle-detection signal Kruskal relies on",
      "Track component count by decrementing only on successful merges — never recompute it",
      "DSU is inherently offline for deletions; reverse the timeline to handle edge removals",
      "Storing a weight/offset to the parent extends DSU to relative-value and bipartiteness queries",
    ],
    problems: [
      { title: "Number of Provinces", url: "https://leetcode.com/problems/number-of-provinces/", difficulty: "Medium", pattern: "count connected components via union" },
      { title: "Redundant Connection", url: "https://leetcode.com/problems/redundant-connection/", difficulty: "Medium", pattern: "find the edge that closes a cycle" },
      { title: "Accounts Merge", url: "https://leetcode.com/problems/accounts-merge/", difficulty: "Medium", pattern: "union by shared email, group by root" },
      { title: "Number of Operations to Make Network Connected", url: "https://leetcode.com/problems/number-of-operations-to-make-network-connected/", difficulty: "Medium", pattern: "components minus 1 vs spare edges" },
      { title: "Number of Islands II", url: "https://leetcode.com/problems/number-of-islands-ii/", difficulty: "Hard", pattern: "incremental DSU, add land and merge neighbors" },
    ],
    videos: [
      { title: "Union Find Introduction", url: "https://www.youtube.com/watch?v=ibjEGG7ylHk", source: "WilliamFiset" },
      { title: "Union Find [Disjoint Set] (full playlist)", url: "https://www.youtube.com/playlist?list=PLDV1Zeh2NRsBI1C-mR6ZhHTyfoEJWlxvq", source: "WilliamFiset" },
      { title: "Disjoint Set Union (DSU) Fully Explained in 1 Hour", url: "https://www.youtube.com/watch?v=L1T4AB8xBcw", source: "YouTube" },
    ],
    articles: [
      { title: "Introduction to Disjoint Set (Union-Find Algorithm)", url: "https://www.geeksforgeeks.org/dsa/introduction-to-disjoint-set-data-structure-or-union-find-algorithm/", source: "GeeksforGeeks" },
      { title: "Disjoint Set Union", url: "https://cp-algorithms.com/data_structures/disjoint_set_union.html", source: "CP-Algorithms" },
      { title: "Disjoint-set data structure", url: "https://en.wikipedia.org/wiki/Disjoint-set_data_structure", source: "Wikipedia" },
    ],
  },

  "shortest-path": {
    id: "shortest-path",
    name: "Shortest Path",
    summary: "Find the minimum-cost route between nodes; pick the algorithm by edge weights and whether negatives exist.",
    overview:
      "## The core idea\n\nShortest-path algorithms compute minimum total edge cost from a source to other nodes. The right tool depends entirely on the **edge weights**. Unweighted: plain **BFS**. Non-negative weights: **Dijkstra** with a min-heap. Negative weights: **Bellman-Ford**. All-pairs on a small dense graph: **Floyd-Warshall**. On a DAG: relax edges in **topological order**.\n\n## How to think\n\nEvery algorithm is built on **relaxation**: if `dist[u] + w(u,v) < dist[v]`, you found a cheaper way to v, so update it. Dijkstra greedily finalizes the closest unsettled node each step — valid only because no negative edge can later undercut a settled distance. Always carry a `dist` array initialized to infinity (0 at the source) and, in Dijkstra, **skip stale heap entries** where the popped distance exceeds the recorded `dist`.\n\n## Variants\n\n- **BFS** — O(V+E) for unit weights; the first visit is optimal.\n- **Dijkstra** — O(E log V) for non-negative weights; lazy deletion via a heap.\n- **Bellman-Ford** — O(V·E); handles negatives and detects negative cycles.\n- **Floyd-Warshall** — O(V³) all-pairs; tiny dense graphs.\n- **0-1 BFS** — deque, O(V+E) when weights are only 0 or 1.\n\n## Edge thinking\n\nThe trap is running Dijkstra with negative edges — it silently returns wrong answers. If negatives are possible, switch to Bellman-Ford. Unreachable nodes keep distance infinity; report -1.",
    recognition: [
      "You need the minimum total cost / time / distance between nodes in a weighted graph",
      "Edges carry positive weights (tolls, times, prices) and you want the cheapest route",
      "Some edges may be negative or you must detect a negative cycle",
      "A grid asks for the path minimizing a maximum or summed cost (effort, time, height)",
      "You need shortest paths between every pair of a small number of nodes",
      "Signals/flights/delays propagate through a network and you want the slowest-but-minimal arrival",
    ],
    whenToUse: [
      "Single-source shortest paths with non-negative weights — Dijkstra",
      "Unweighted shortest path — BFS",
      "Graphs with negative edges or negative-cycle detection — Bellman-Ford",
      "All-pairs shortest paths on a small graph (V ≲ 400) — Floyd-Warshall",
    ],
    whenNotToUse: [
      "All edges have equal weight — Dijkstra is overkill, use BFS",
      "You need every path or to count paths, not just the cheapest — different DP/search",
      "The graph is enormous and you only need source-to-target — consider A* with a heuristic",
    ],
    patterns: [
      { name: "Dijkstra (min-heap)", note: "pop the closest unsettled node; relax neighbors; skip stale (d > dist[u]) pops" },
      { name: "Bellman-Ford", note: "relax all edges V-1 times; a V-th improving pass means a negative cycle" },
      { name: "Floyd-Warshall", note: "triple loop k,i,j: dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])" },
      { name: "0-1 BFS / DAG relaxation", note: "deque for 0/1 weights; relax in topo order on a DAG for linear time" },
    ],
    complexity: [
      { operation: "Dijkstra (binary heap)", time: "O(E log V)", space: "O(V + E)", note: "non-negative weights only" },
      { operation: "BFS (unweighted)", time: "O(V + E)", space: "O(V)", note: "unit edge weights" },
      { operation: "Bellman-Ford", time: "O(V * E)", space: "O(V)", note: "handles negatives, detects negative cycle" },
      { operation: "Floyd-Warshall", time: "O(V^3)", space: "O(V^2)", note: "all pairs, small dense graphs" },
    ],
    edgeCases: [
      "Unreachable target — distance stays infinity, return -1",
      "Negative edge with Dijkstra — wrong answer; must use Bellman-Ford",
      "Negative cycle — shortest path is undefined / minus infinity, detect it",
      "Source equals target — distance 0",
      "Self-loops and parallel edges — keep only the cheapest where it matters",
      "Disconnected graph — some nodes remain at infinity",
    ],
    commonMistakes: [
      "Running Dijkstra on a graph with negative edge weights",
      "Not skipping stale heap entries, so finalized nodes get reprocessed and timing degrades",
      "Initializing dist to 0 everywhere instead of infinity (only the source is 0)",
      "Forgetting Bellman-Ford needs exactly V-1 relaxation rounds before the cycle check",
      "Pushing into the heap without storing the distance, so you can't detect staleness",
      "Treating an unreachable node's infinity as a real number in later arithmetic",
    ],
    template: {
      language: "python",
      code: "import heapq\n\ndef dijkstra(n, adj, src):\n    # adj[u] = list of (v, weight); weights >= 0\n    dist = [float('inf')] * n\n    dist[src] = 0\n    pq = [(0, src)]                  # (distance, node)\n    while pq:\n        d, u = heapq.heappop(pq)\n        if d > dist[u]:\n            continue                 # stale entry, skip\n        for v, w in adj[u]:\n            nd = d + w\n            if nd < dist[v]:\n                dist[v] = nd\n                heapq.heappush(pq, (nd, v))\n    return dist                      # inf entries are unreachable",
    },
    observations: [
      "Dijkstra's greedy choice is valid only because non-negative edges can never improve a settled distance",
      "The 'skip if d > dist[u]' check is what makes a lazy-deletion heap correct and fast",
      "Bellman-Ford needs V-1 passes because the longest simple path has at most V-1 edges",
      "0-1 BFS with a deque matches Dijkstra's answer in linear time when weights are only 0 or 1",
      "Many grid problems (minimum effort, swim in water) are Dijkstra where 'distance' is a max, not a sum",
    ],
    problems: [
      { title: "Network Delay Time", url: "https://leetcode.com/problems/network-delay-time/", difficulty: "Medium", pattern: "single-source Dijkstra, answer is the max distance" },
      { title: "Path with Minimum Effort", url: "https://leetcode.com/problems/path-with-minimum-effort/", difficulty: "Medium", pattern: "Dijkstra on a grid minimizing the maximum step" },
      { title: "Cheapest Flights Within K Stops", url: "https://leetcode.com/problems/cheapest-flights-within-k-stops/", difficulty: "Medium", pattern: "Bellman-Ford bounded to K+1 relaxations" },
      { title: "Swim in Rising Water", url: "https://leetcode.com/problems/swim-in-rising-water/", difficulty: "Hard", pattern: "Dijkstra where cost is the running maximum cell height" },
      { title: "Minimum Cost to Make at Least One Valid Path in a Grid", url: "https://leetcode.com/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid/", difficulty: "Hard", pattern: "0-1 BFS with a deque" },
    ],
    videos: [
      { title: "Dijkstra's Shortest Path Algorithm | Graph Theory", url: "https://www.youtube.com/watch?v=pSqmAO-m7Lk", source: "WilliamFiset" },
      { title: "G-32. Dijkstra's Algorithm - Using Priority Queue - C++ and Java", url: "https://www.youtube.com/watch?v=V6H1qAeB-l4", source: "takeUforward (Striver)" },
      { title: "Network Delay Time - Dijkstra's algorithm - Leetcode 743", url: "https://www.youtube.com/watch?v=EaphyqKU4PQ", source: "NeetCode" },
    ],
    articles: [
      { title: "Dijkstra's Algorithm", url: "https://cp-algorithms.com/graph/dijkstra.html", source: "CP-Algorithms" },
      { title: "Dijkstra's Shortest Path Algorithm", url: "https://www.geeksforgeeks.org/dsa/dijkstras-shortest-path-algorithm-greedy-algo-7/", source: "GeeksforGeeks" },
      { title: "Shortest path problem", url: "https://en.wikipedia.org/wiki/Shortest_path_problem", source: "Wikipedia" },
    ],
  },

  "mst": {
    id: "mst",
    name: "Minimum Spanning Tree",
    summary: "Connect all nodes of a weighted graph with the cheapest possible set of edges and no cycles.",
    overview:
      "## The core idea\n\nA **spanning tree** of a connected, undirected graph picks `V-1` edges that connect every vertex with no cycle. The **minimum** spanning tree is the one with the smallest total edge weight. Two greedy algorithms find it: **Kruskal** (sort edges, add the cheapest that doesn't form a cycle) and **Prim** (grow one tree outward, always taking the cheapest edge leaving it).\n\n## How to think\n\nBoth rely on the **cut property**: for any way of splitting vertices into two groups, the lightest edge crossing the split is safe to include in some MST. Kruskal applies this globally — sort all edges, use **Union-Find** to reject edges whose endpoints are already connected. Prim applies it locally — keep a min-heap of edges touching the current tree and repeatedly pull the cheapest edge to a node not yet in the tree.\n\n## Variants\n\n- **Kruskal** — sort + DSU; great for sparse / edge-list graphs.\n- **Prim (lazy)** — min-heap of candidate edges; simple, may hold stale edges.\n- **Prim (eager)** — indexed priority queue, one entry per node; less memory.\n- **Boruvka** — each round, every component grabs its cheapest edge; parallel-friendly.\n\n## Edge thinking\n\nMST is defined only for a **connected** graph; if it isn't connected you get a minimum spanning *forest* and can't connect everyone — detect this (Kruskal adds fewer than V-1 edges). Equal-weight edges mean the MST may not be unique, but its total weight always is.",
    recognition: [
      "You must connect all nodes/cities/points at minimum total cost with no redundant links",
      "The problem reduces to choosing the cheapest cycle-free set of edges covering every vertex",
      "Wiring, road, pipeline, or cable layout minimizing total length",
      "Build a network where any extra edge would be wasteful",
      "Points on a plane to connect with Manhattan/Euclidean distance edges",
      "Clustering by repeatedly joining the closest groups (single-linkage)",
    ],
    whenToUse: [
      "Minimize the total weight needed to connect every vertex once",
      "Sparse, edge-list graphs — Kruskal with Union-Find",
      "Dense graphs given as an adjacency matrix — Prim with a heap",
      "Cost-minimal connectivity where any cycle is pure waste",
    ],
    whenNotToUse: [
      "You need shortest paths between specific nodes — MST edges are not shortest paths",
      "Edges are directed — MST is for undirected graphs (use arborescence algorithms instead)",
      "You must minimize the maximum edge only, or satisfy flow/capacity constraints",
    ],
    patterns: [
      { name: "Kruskal + Union-Find", note: "sort edges ascending; add an edge iff its endpoints are in different DSU sets" },
      { name: "Prim (lazy heap)", note: "push all edges from a newly added node; pop cheapest, skip if endpoint already in tree" },
      { name: "Prim (eager)", note: "indexed PQ keyed by node with its best connecting cost; decrease-key on improvement" },
      { name: "Cut property check", note: "the lightest edge crossing any vertex partition is safe for some MST" },
    ],
    complexity: [
      { operation: "Kruskal", time: "O(E log E)", space: "O(V)", note: "sort dominates; DSU ops are near-constant" },
      { operation: "Prim (binary heap)", time: "O(E log V)", space: "O(V + E)", note: "good for sparse graphs" },
      { operation: "Prim (dense / matrix)", time: "O(V^2)", space: "O(V)", note: "no heap; scan for the closest node each step" },
    ],
    edgeCases: [
      "Disconnected graph — no spanning tree; Kruskal ends with fewer than V-1 edges",
      "Single vertex — MST has 0 edges and weight 0",
      "Equal-weight edges — MST is non-unique but total weight is fixed",
      "Parallel edges — keep only the cheapest between a pair",
      "Self-loops — never part of an MST, skip them",
      "Already a tree — the MST is the graph itself",
    ],
    commonMistakes: [
      "In Kruskal, adding an edge without the Union-Find cycle check, creating a cycle",
      "In Prim, re-adding a node already in the tree because stale heap entries weren't skipped",
      "Forgetting MST needs a connected graph and reporting a wrong total when it isn't",
      "Stopping before V-1 edges are chosen (or one short and never noticing)",
      "Confusing MST with shortest-path trees — they are generally different",
      "Sorting edges by the wrong key or not by weight at all in Kruskal",
    ],
    template: {
      language: "python",
      code: "def kruskal(n, edges):\n    # edges: list of (weight, u, v); returns total MST weight or -1 if disconnected\n    parent = list(range(n))\n\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]\n            x = parent[x]\n        return x\n\n    edges.sort()                       # ascending by weight\n    total, used = 0, 0\n    for w, u, v in edges:\n        ru, rv = find(u), find(v)\n        if ru != rv:                   # no cycle -> safe edge\n            parent[ru] = rv\n            total += w\n            used += 1\n            if used == n - 1:\n                break\n    return total if used == n - 1 else -1",
    },
    observations: [
      "Both Kruskal and Prim are correct because of the cut property: the lightest crossing edge is always safe",
      "Kruskal is essentially Union-Find with edges processed in sorted order",
      "An MST has exactly V-1 edges; ending with fewer means the graph was disconnected",
      "MST minimizes total weight, never the path between two specific nodes — don't confuse it with Dijkstra",
      "When all weights are distinct the MST is unique; ties allow several MSTs of equal total weight",
    ],
    problems: [
      { title: "Find Critical and Pseudo-Critical Edges in MST", url: "https://leetcode.com/problems/find-critical-and-pseudo-critical-edges-in-minimum-spanning-tree/", difficulty: "Hard", pattern: "repeated Kruskal forcing/excluding edges" },
      { title: "Min Cost to Connect All Points", url: "https://leetcode.com/problems/min-cost-to-connect-all-points/", difficulty: "Medium", pattern: "MST over complete graph of Manhattan distances" },
      { title: "Connecting Cities With Minimum Cost", url: "https://leetcode.com/problems/connecting-cities-with-minimum-cost/", difficulty: "Medium", pattern: "classic Kruskal, detect disconnected graph" },
      { title: "Optimize Water Distribution in a Village", url: "https://leetcode.com/problems/optimize-water-distribution-in-a-village/", difficulty: "Hard", pattern: "virtual node for wells, then MST" },
      { title: "The Earliest Moment When Everyone Become Friends", url: "https://leetcode.com/problems/the-earliest-moment-when-everyone-become-friends/", difficulty: "Medium", pattern: "Union-Find over time-sorted edges until fully connected" },
    ],
    videos: [
      { title: "Eager Prim's Minimum Spanning Tree Algorithm | Graph Theory", url: "https://www.youtube.com/watch?v=xq3ABa-px_g", source: "WilliamFiset" },
      { title: "Kruskal's Algorithm Visually Explained (Minimum Spanning Tree)", url: "https://www.youtube.com/watch?v=OxfTT8slSLs", source: "YouTube" },
      { title: "WilliamFiset - Graph Theory Algorithms (channel)", url: "https://www.youtube.com/channel/UCD8yeTczadqdARzQUp29PJw", source: "WilliamFiset" },
    ],
    articles: [
      { title: "Kruskal's Minimum Spanning Tree (MST) Algorithm", url: "https://www.geeksforgeeks.org/dsa/kruskals-minimum-spanning-tree-algorithm-greedy-algo-2/", source: "GeeksforGeeks" },
      { title: "Minimum spanning tree - Kruskal's algorithm", url: "https://cp-algorithms.com/graph/mst_kruskal.html", source: "CP-Algorithms" },
      { title: "Minimum spanning tree", url: "https://en.wikipedia.org/wiki/Minimum_spanning_tree", source: "Wikipedia" },
    ],
  },

  "greedy": {
    id: "greedy",
    name: "Greedy",
    summary: "Build a solution by always taking the locally best choice, when that provably reaches a global optimum.",
    overview:
      "## The core idea\n\nA greedy algorithm makes the choice that looks best **right now** and never reconsiders it. This is fast and simple, but it is only *correct* when the problem has the **greedy-choice property** (a local optimum extends to a global one) and **optimal substructure**. The hard part is never the code — it's proving the greedy choice is safe.\n\n## How to think\n\nFind the right **ordering or priority**: sort by finish time (interval scheduling), by ratio (fractional knapsack), by deadline, or process with a heap. Then ask the killer question: **does taking the best item now ever block a better overall solution?** If you can argue 'no' via an **exchange argument** (any optimal solution can be transformed to include the greedy choice without getting worse), greedy works. If you can't, suspect **dynamic programming** instead.\n\n## Variants\n\n- **Sort then sweep** — order by a key, make one linear pass (intervals, jumps).\n- **Heap-driven greedy** — repeatedly pull the current extreme (task scheduler, merge cost).\n- **Exchange-argument greedy** — pick the choice provably swappable into any optimum.\n- **Greedy + invariant** — maintain a running quantity (farthest reach, current end).\n\n## Edge thinking\n\nThe number-one mistake is **assuming** greedy works without proof — many problems (0/1 knapsack, coin change with odd denominations) need DP. Counterexamples are your friend: try to break your greedy on a small input before trusting it. Empty input and single-element cases should be handled explicitly.",
    recognition: [
      "You can sort the input by a natural key (end time, ratio, deadline) and sweep once",
      "The problem asks for a max/min count of non-overlapping or compatible items",
      "At each step there's an obvious 'best' local move and reversing it never seems to help",
      "Activity/interval selection, scheduling, or assigning resources to requests",
      "You repeatedly need the current largest/smallest element (heap-driven choice)",
      "Fractional / divisible resources where you can take the best portion first",
    ],
    whenToUse: [
      "The greedy-choice property holds and you can prove it (exchange argument)",
      "Interval scheduling, partitioning, or merging where local choices compose",
      "You need a fast O(n log n) solution and a correct ordering exists",
      "Problems with divisible resources (fractional knapsack)",
    ],
    whenNotToUse: [
      "Choices interact so a locally best move can block the optimum — use DP (0/1 knapsack, coin change with arbitrary coins)",
      "You must consider combinations or backtrack — greedy can't undo",
      "You can construct a counterexample where greedy underperforms",
    ],
    patterns: [
      { name: "Sort then sweep", note: "order by the decisive key, then take items in one linear pass while feasible" },
      { name: "Interval scheduling", note: "sort by end time; keep an interval iff it starts after the last chosen end" },
      { name: "Heap-driven greedy", note: "repeatedly pop the current max/min to make the next best move" },
      { name: "Exchange argument", note: "prove any optimal solution can swap in the greedy choice without loss" },
    ],
    complexity: [
      { operation: "Sort + single sweep", time: "O(n log n)", space: "O(1) to O(n)", note: "sort dominates; pass is linear" },
      { operation: "Heap-driven greedy", time: "O(n log n)", space: "O(n)", note: "each push/pop is log n" },
      { operation: "Already-ordered sweep", time: "O(n)", space: "O(1)", note: "when input arrives in the needed order" },
    ],
    edgeCases: [
      "Empty input — return the identity answer (0, empty list)",
      "Single element — trivially optimal",
      "All elements identical — ordering ties don't change the result",
      "Ties on the sort key — make sure the tie-break doesn't break correctness",
      "Negative values where the greedy key assumes positivity",
      "Input where greedy is tempting but actually wrong — verify with a counterexample first",
    ],
    commonMistakes: [
      "Assuming greedy is correct without an exchange-argument proof or a counterexample check",
      "Sorting by the wrong key (e.g. by start time instead of end time for interval scheduling)",
      "Using greedy on 0/1 knapsack or general coin change where DP is required",
      "Forgetting to handle ties on the sort key, leading to suboptimal picks",
      "Mutating the data mid-sweep so the chosen ordering becomes invalid",
      "Stopping early or off-by-one on the boundary between 'take' and 'skip'",
    ],
    template: {
      language: "python",
      code: "def max_non_overlapping(intervals):\n    # Classic interval scheduling: keep the most non-overlapping intervals.\n    if not intervals:\n        return 0\n    intervals.sort(key=lambda x: x[1])     # sort by END time\n    count = 0\n    last_end = float('-inf')\n    for start, end in intervals:\n        if start >= last_end:              # compatible with last chosen\n            count += 1\n            last_end = end                 # greedily commit\n    return count",
    },
    observations: [
      "Greedy is trivial to code; the entire difficulty is proving the local choice is globally safe",
      "For interval problems, sorting by end time (not start time) is the choice that admits a clean exchange argument",
      "If a small counterexample breaks your greedy, the problem almost certainly needs DP",
      "Fractional knapsack is greedy (take best ratio); 0/1 knapsack is DP — divisibility is the deciding factor",
      "An exchange argument and an induction on the greedy step are the two standard correctness proofs",
    ],
    problems: [
      { title: "Assign Cookies", url: "https://leetcode.com/problems/assign-cookies/", difficulty: "Easy", pattern: "sort both, two-pointer greedy matching" },
      { title: "Jump Game", url: "https://leetcode.com/problems/jump-game/", difficulty: "Medium", pattern: "track farthest reachable index in one sweep" },
      { title: "Non-overlapping Intervals", url: "https://leetcode.com/problems/non-overlapping-intervals/", difficulty: "Medium", pattern: "sort by end, count keep vs remove" },
      { title: "Task Scheduler", url: "https://leetcode.com/problems/task-scheduler/", difficulty: "Medium", pattern: "heap / math greedy on most frequent task" },
      { title: "Candy", url: "https://leetcode.com/problems/candy/", difficulty: "Hard", pattern: "two greedy passes, left-to-right then right-to-left" },
    ],
    videos: [
      { title: "3. Greedy Method - Introduction", url: "https://www.youtube.com/watch?v=ARvQcqJ_-NY", source: "Abdul Bari" },
      { title: "Greedy Algorithms In-depth Explanation and Playlist (for Coding Interviews)", url: "https://www.youtube.com/watch?v=-WTslqPbj7I", source: "NeetCode" },
      { title: "L5. Jump Game - II | Greedy Algorithm Playlist", url: "https://www.youtube.com/watch?v=7SBVnw7GSTk", source: "takeUforward (Striver)" },
    ],
    articles: [
      { title: "Greedy Algorithms", url: "https://www.geeksforgeeks.org/dsa/greedy-algorithms/", source: "GeeksforGeeks" },
      { title: "Greedy algorithm", url: "https://en.wikipedia.org/wiki/Greedy_algorithm", source: "Wikipedia" },
      { title: "Greedy Algorithms", url: "https://usaco.guide/bronze/intro-greedy", source: "USACO Guide" },
    ],
  },
};
