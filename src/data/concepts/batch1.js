// Concepts: Time Complexity & Big O · Arrays · Strings · Hash Maps / Hash Sets · Two Pointers · Sliding Window
export const BATCH = {
  "big-o": {
    id: "big-o",
    name: "Time Complexity & Big O",
    summary: "A language for describing how an algorithm's cost grows with input size, ignoring constants.",
    overview:
      "## The core idea\n\nBig O describes the **asymptotic** growth of an algorithm's running time or memory as the input size `n` grows. You drop constant factors and lower-order terms, so `O(2n + 5)` becomes `O(n)` and `O(n^2 + n)` becomes `O(n^2)`. What matters is the **dominant term** as `n` gets large, because that is what decides whether a solution passes or times out.\n\n## How to think\n\nDon't memorize formulas — **count how the work grows**. Ask: how many times does the inner statement run as a function of `n`? A single loop over `n` items is `O(n)`. A loop nested inside another is usually `O(n^2)`. Halving the search space each step is `O(log n)`. Branching recursion that splits and recombines is often `O(n log n)`. Then read the constraints **backwards**: `n <= 20` hints exponential/bitmask, `n <= 500` hints `O(n^3)`, `n <= 5000` hints `O(n^2)`, `n <= 10^6` demands `O(n)` or `O(n log n)`. Judges allow roughly `10^8` simple ops per second.\n\n## Common patterns\n\n- **Sequential vs nested**: two loops in a row are `O(n + m)`; nesting **multiplies** to `O(n*m)`.\n- **Divide and conquer**: recurrences via the Master Theorem (e.g. `T(n)=2T(n/2)+O(n)` -> `O(n log n)`).\n- **Amortized**: dynamic-array append is `O(1)` amortized despite occasional `O(n)` resizes.\n\n## Edge thinking\n\nAlways state **both** time and space, and remember the recursion stack counts as space. The biggest trap: hidden costs like string concatenation in a loop being `O(n^2)`, or a `O(1)` hash lookup degrading to `O(n)` on adversarial collisions.",
    recognition: [
      "The problem gives explicit bounds like n <= 10^5, hinting at the target complexity",
      "A correct solution times out (TLE) and you must find the dominant loop",
      "You must justify why one of two correct approaches is fast enough",
      "You see nested loops, recursion, or repeated work and must total the cost",
      "Memory limits are tight and you must account for arrays plus recursion stack",
      "You need to choose between a faster complex algorithm and a simpler slower one",
    ],
    whenToUse: [
      "Choosing an algorithm before coding, using constraints to back into the target Big O",
      "Diagnosing a TLE and deciding which part of the code to optimize",
      "Estimating auxiliary memory for arrays, recursion stacks, or hash maps",
      "Comparing the asymptotic trade-off between two candidate solutions",
    ],
    whenNotToUse: [
      "Micro-optimizing constants when the asymptotic class is already optimal",
      "Treating Big O as exact runtime; O(n) with a huge constant can lose to O(n log n)",
      "Reasoning about tiny fixed inputs where any approach trivially passes",
    ],
    patterns: [
      { name: "Single pass", note: "one loop over n elements -> O(n) time, O(1) extra space" },
      { name: "Nested loops", note: "loop inside loop over the same input -> usually O(n^2)" },
      { name: "Divide and halve", note: "cut the problem in half each step -> O(log n)" },
      { name: "Divide and conquer", note: "split, recurse on parts, merge -> often O(n log n)" },
      { name: "Exhaustive search", note: "subsets O(2^n), permutations O(n!); only tiny n" },
    ],
    complexity: [
      { operation: "Single pass over array", time: "O(n)", space: "O(1)", note: "one loop, constant extra space" },
      { operation: "Nested loop over all pairs", time: "O(n^2)", space: "O(1)", note: "watch for n > ~5000" },
      { operation: "Binary search / halve each step", time: "O(log n)", space: "O(1)", note: "needs sorted or monotonic data" },
      { operation: "Sort then process", time: "O(n log n)", space: "O(n)", note: "comparison-sort lower bound" },
      { operation: "Subset / bitmask enumeration", time: "O(2^n)", space: "O(n)", note: "feasible only for n <= ~22" },
    ],
    edgeCases: [
      "Empty input (n = 0) — the dominant term vanishes but the algorithm must still terminate",
      "Two different size parameters: O(n + m) is not O(n) when m can dominate",
      "Recursion depth counts as O(depth) stack space even when no array is allocated",
      "Amortized O(1) hides occasional O(n) operations (array resize, hash rehash)",
      "Output size can dominate: producing all pairs is at least O(n^2) regardless of cleverness",
    ],
    commonMistakes: [
      "Forgetting that repeated string concatenation in a loop is O(n^2), not O(n)",
      "Claiming hash-map lookup is O(1) without noting worst-case O(n) on collisions",
      "Ignoring O(n) recursion-stack space when claiming 'O(1) space'",
      "Confusing the count of loop iterations with the work done inside each iteration",
      "Dropping the wrong term: keeping a lower-order term or losing a second variable",
      "Assuming O(n log n) and O(n) are interchangeable under tight time limits",
      "Treating log base as significant — it only changes a constant factor",
    ],
    template: {
      language: "python",
      code: "# Estimate complexity by counting how the work grows.\n\n# O(n): one pass\ntotal = 0\nfor x in nums:\n    total += x\n\n# O(n^2): all pairs (nesting multiplies)\nfor i in range(n):\n    for j in range(i + 1, n):\n        check(nums[i], nums[j])\n\n# O(log n): halve the search space each step\nlo, hi = 0, n - 1\nwhile lo <= hi:\n    mid = (lo + hi) // 2\n    if nums[mid] == target:\n        break\n    elif nums[mid] < target:\n        lo = mid + 1\n    else:\n        hi = mid - 1\n\n# O(n log n): the sort dominates the linear scan after it\nnums.sort()\nfor x in nums:\n    use(x)",
    },
    observations: [
      "Constraints are a hint: n <= 10^5 almost always means O(n) or O(n log n) is intended",
      "Two sequential loops are O(n + m); only nesting multiplies the cost",
      "log base is irrelevant in Big O because logarithms differ only by a constant factor",
      "Amortized analysis explains why append/rehash average to O(1) despite spikes",
      "Big O is an upper bound; Big Theta is the tight bound and is what people usually mean",
    ],
    problems: [
      { title: "Running Sum of 1d Array", url: "https://leetcode.com/problems/running-sum-of-1d-array/", difficulty: "Easy", pattern: "single O(n) pass with running aggregate" },
      { title: "Squares of a Sorted Array", url: "https://leetcode.com/problems/squares-of-a-sorted-array/", difficulty: "Easy", pattern: "O(n) two-pointer beats the O(n log n) sort" },
      { title: "Search Insert Position", url: "https://leetcode.com/problems/search-insert-position/", difficulty: "Easy", pattern: "O(log n) binary search on sorted input" },
      { title: "Sort an Array", url: "https://leetcode.com/problems/sort-an-array/", difficulty: "Medium", pattern: "O(n log n) divide-and-conquer (merge/quick sort)" },
      { title: "Subsets", url: "https://leetcode.com/problems/subsets/", difficulty: "Medium", pattern: "O(2^n) exhaustive enumeration only feasible for small n" },
    ],
    videos: [
      { title: "Asymptotic Notations: Big Oh, Omega, Theta (Part 1)", url: "https://www.youtube.com/watch?v=A03oI0znAoc", source: "Abdul Bari" },
      { title: "Asymptotic Notations: Big Oh, Omega, Theta (Part 2)", url: "https://www.youtube.com/watch?v=Nd0XDY-jVHs", source: "Abdul Bari" },
      { title: "Algorithms and Data Structures Tutorial - Full Course for Beginners", url: "https://www.youtube.com/watch?v=8hly31xKli0", source: "freeCodeCamp" },
    ],
    articles: [
      { title: "Analysis of Algorithms (Big-O Analysis)", url: "https://www.geeksforgeeks.org/analysis-of-algorithms-big-o-analysis/", source: "GeeksforGeeks" },
      { title: "Big O notation", url: "https://en.wikipedia.org/wiki/Big_O_notation", source: "Wikipedia" },
      { title: "Time Complexity (Competitive Programming)", url: "https://usaco.guide/bronze/time-comp", source: "USACO Guide" },
    ],
  },

  "arrays": {
    id: "arrays",
    name: "Arrays",
    summary: "Contiguous, index-addressable storage — the foundation almost every other technique builds on.",
    overview:
      "## The core idea\n\nAn array stores elements in **contiguous memory**, giving `O(1)` access by index and cache-friendly sequential scans. That makes reads, in-place swaps, and running aggregates extremely cheap. The cost is structural change: inserting or deleting in the middle is `O(n)` because everything after the gap must shift.\n\n## How to think\n\nMost array problems collapse into a small menu of **moves**. Ask: can I solve this in **one linear scan** while tracking something (max so far, a count, a running sum)? Can two indices walk the array — toward each other, or one chasing the other? Can I **transform in place** to hit `O(1)` extra space? Do I need a **prefix sum** so range queries become subtraction? Recognizing which move applies is most of the battle; the coding is usually short once you've named the pattern.\n\n## Common patterns\n\n- **Single scan + accumulator** — Kadane's max-subarray, running max/min.\n- **Two pointers** — opposite ends (sorted pair search) or fast/slow (in-place dedupe).\n- **Prefix sums** — precompute cumulative totals for `O(1)` range sums.\n- **In-place rearrangement** — partition, rotate, move-zeroes without extra arrays.\n\n## Edge thinking\n\nThe edges break more solutions than the core logic: empty arrays, single elements, and **off-by-one** loop bounds. Decide up front whether you are **allowed to mutate** the input, and whether duplicates or negative numbers change your invariant — Kadane's, for instance, must handle an all-negative array correctly.",
    recognition: [
      "Input is a list/array and you must read or transform it by position",
      "The problem is solvable with one or two linear scans",
      "In-place modification with O(1) extra space is requested or rewarded",
      "You need running aggregates (max-so-far, count, sum) while iterating",
      "Order matters and you cannot freely reorder without losing information",
      "Repeated range-sum queries appear, suggesting a prefix-sum precompute",
    ],
    whenToUse: [
      "Random access by index in O(1) is needed",
      "Data is fixed-size or append-mostly rather than inserted in the middle",
      "You want cache-friendly sequential iteration",
      "In-place rearrangement (rotate, partition, dedupe sorted) is the goal",
    ],
    whenNotToUse: [
      "Frequent insertion/deletion in the middle — use a linked list or balanced structure",
      "Membership tests dominate — a hash set gives O(1) average vs O(n) scan",
      "You need ordered range queries with updates — a tree/Fenwick fits better",
    ],
    patterns: [
      { name: "Single scan + accumulator", note: "one pass tracking max/min/sum/count (e.g. Kadane's)" },
      { name: "Two pointers", note: "opposite-end or fast/slow indices walk the array" },
      { name: "Prefix sums", note: "precompute cumulative totals; range sum = pre[r+1]-pre[l]" },
      { name: "In-place rearrange", note: "partition / move-zeroes / rotate with O(1) extra space" },
    ],
    complexity: [
      { operation: "Access by index", time: "O(1)", space: "O(1)", note: "contiguous memory" },
      { operation: "Linear scan", time: "O(n)", space: "O(1)", note: "the workhorse for most problems" },
      { operation: "Insert/delete in middle", time: "O(n)", space: "O(1)", note: "shifts all later elements" },
      { operation: "Append (dynamic array)", time: "O(1) amortized", space: "O(1)", note: "occasional O(n) resize" },
      { operation: "Build + query prefix sums", time: "O(n) build, O(1) query", space: "O(n)", note: "trades memory for fast range sums" },
    ],
    edgeCases: [
      "Empty array — guard before reading index 0",
      "Single element — many two-pointer/window loops must still be correct",
      "All-negative values (Kadane's must not return 0)",
      "Off-by-one on loop bounds and on prefix-sum index alignment",
      "Duplicate values when the logic assumes distinct elements",
      "Integer overflow in sums/products in fixed-width languages (not Python, but state it)",
    ],
    commonMistakes: [
      "Mutating the input while iterating, invalidating indices",
      "Off-by-one in range(len(a)) vs range(len(a)-1) for adjacent-pair logic",
      "Initializing max to 0 instead of -inf or nums[0], breaking all-negative cases",
      "Using O(n) list.pop(0) / insert(0) in a loop, turning it into O(n^2)",
      "Forgetting prefix arrays are length n+1 with pre[0]=0",
      "Assuming the array is sorted when it is not (two-pointer pair search needs sorting)",
    ],
    template: {
      language: "python",
      code: "# Prefix sums for O(1) range-sum queries, and Kadane's for max subarray.\nfrom math import inf\n\ndef prefix_sums(nums):\n    pre = [0] * (len(nums) + 1)\n    for i, x in enumerate(nums):\n        pre[i + 1] = pre[i] + x\n    # sum of nums[l..r] inclusive:\n    range_sum = lambda l, r: pre[r + 1] - pre[l]\n    return range_sum\n\ndef max_subarray(nums):\n    best = cur = -inf\n    for x in nums:\n        cur = max(x, cur + x)   # extend or restart\n        best = max(best, cur)\n    return best",
    },
    observations: [
      "A prefix-sum array of length n+1 with pre[0]=0 removes all left-edge special cases",
      "Kadane's is dynamic programming in disguise: cur = best subarray ending here",
      "Two opposite-end pointers only work after sorting (or on an already-sorted array)",
      "list.pop(0) is O(n); use collections.deque when you need cheap front operations",
      "Reading right-to-left or precomputing suffixes often removes a nested loop",
    ],
    problems: [
      { title: "Two Sum", url: "https://leetcode.com/problems/two-sum/", difficulty: "Easy", pattern: "hash map of complements in one pass" },
      { title: "Best Time to Buy and Sell Stock", url: "https://leetcode.com/problems/best-time-to-buy-and-sell-stock/", difficulty: "Easy", pattern: "single scan tracking min-so-far" },
      { title: "Maximum Subarray", url: "https://leetcode.com/problems/maximum-subarray/", difficulty: "Medium", pattern: "Kadane's running maximum" },
      { title: "Product of Array Except Self", url: "https://leetcode.com/problems/product-of-array-except-self/", difficulty: "Medium", pattern: "prefix/suffix products, no division" },
      { title: "Rotate Array", url: "https://leetcode.com/problems/rotate-array/", difficulty: "Medium", pattern: "in-place reversal trick, O(1) space" },
    ],
    videos: [
      { title: "NeetCode 150 Ep.1: Arrays & Hashing Explained", url: "https://www.youtube.com/watch?v=IiDuXLqV6e4", source: "NeetCode" },
      { title: "Arrays & Hashing (NeetCode 150 playlist)", url: "https://www.youtube.com/playlist?list=PLALUz6Z8Un2ew_yN3UAce8bOA25P5kaUl", source: "NeetCode" },
      { title: "Algorithms and Data Structures Tutorial - Full Course for Beginners", url: "https://www.youtube.com/watch?v=8hly31xKli0", source: "freeCodeCamp" },
    ],
    articles: [
      { title: "Introduction to Arrays - Data Structure and Algorithm Tutorials", url: "https://www.geeksforgeeks.org/introduction-to-arrays-data-structure-and-algorithm-tutorials/", source: "GeeksforGeeks" },
      { title: "Prefix Sums", url: "https://usaco.guide/silver/prefix-sums", source: "USACO Guide" },
      { title: "Array data structure", url: "https://en.wikipedia.org/wiki/Array_(data_structure)", source: "Wikipedia" },
    ],
  },

  "strings": {
    id: "strings",
    name: "Strings",
    summary: "Sequences of characters — arrays with their own immutability traps and pattern toolkit.",
    overview:
      "## The core idea\n\nA string is essentially an **array of characters**, so every array technique applies — scanning, two pointers, sliding windows. The twist is **immutability**: in Python (and Java) a string can't be mutated in place, so naive concatenation in a loop is secretly `O(n^2)`. Build with a list and `''.join(...)` instead.\n\n## How to think\n\nFirst classify the task. Is it **frequency-based** (anagrams, character counts)? Reach for `collections.Counter` or a fixed-size 26-element array. Is it about **structure/order** (palindrome, reversal)? Use two pointers from the ends. Is it a **substring/subsequence search**? That's sliding window or a matching algorithm (KMP, Rabin-Karp). Is it **parsing/tokenizing**? Walk a single pointer and group runs. Naming the category tells you the data structure before you write a line.\n\n## Common patterns\n\n- **Counting / frequency** — `Counter`, or `[0]*26` for lowercase-only inputs (compare via equality).\n- **Two pointers** — palindrome checks, reversing, merging.\n- **Sliding window** — longest/shortest substring with a constraint.\n- **Pattern matching** — KMP for `O(n+m)` exact search; rolling hash for many comparisons.\n\n## Edge thinking\n\nMind the **alphabet**: ASCII vs Unicode, case sensitivity, and whether to skip non-alphanumerics (valid-palindrome). Empty strings and single characters are common trip-ups. And never forget the immutability cost — the difference between `O(n)` and `O(n^2)` is often just `join` vs `+=`.",
    recognition: [
      "Input is text and you compare, count, or search characters",
      "Anagram / permutation questions (frequency signatures match)",
      "Palindrome or reversal where two ends move inward",
      "Longest/shortest substring satisfying a constraint (window)",
      "You must find or count occurrences of a pattern inside text",
      "Parsing tokens, runs, or delimiters in a single scan",
    ],
    whenToUse: [
      "Frequency comparison via Counter or a 26-slot array",
      "Two-pointer palindrome / reversal checks",
      "Substring problems that reduce to a sliding window",
      "Exact pattern search where KMP or rolling hash gives linear time",
    ],
    whenNotToUse: [
      "Heavy in-place character edits in immutable-string languages — convert to a list first",
      "Fuzzy/edit-distance matching — that's dynamic programming, not simple scanning",
      "Huge-alphabet frequency work where a fixed array wastes space — use a dict",
    ],
    patterns: [
      { name: "Frequency count", note: "Counter or [0]*26; anagrams share an equal signature" },
      { name: "Two pointers", note: "ends move inward for palindrome / reverse / merge" },
      { name: "Sliding window", note: "expand right, shrink left to keep a substring valid" },
      { name: "Pattern matching", note: "KMP prefix function or Rabin-Karp rolling hash, O(n+m)" },
    ],
    complexity: [
      { operation: "Index / length", time: "O(1)", space: "O(1)", note: "strings are indexable" },
      { operation: "Single scan / build with join", time: "O(n)", space: "O(n)", note: "use ''.join, never += in a loop" },
      { operation: "Naive concatenation in a loop", time: "O(n^2)", space: "O(n)", note: "the classic immutability trap" },
      { operation: "Sort characters", time: "O(n log n)", space: "O(n)", note: "anagram key via sorting" },
      { operation: "KMP / Rabin-Karp search", time: "O(n + m)", space: "O(m)", note: "exact substring match" },
    ],
    edgeCases: [
      "Empty string and single-character string",
      "Case sensitivity — normalize with .lower() when required",
      "Non-alphanumeric characters to skip (valid palindrome)",
      "Unicode vs ASCII; the 26-slot trick assumes lowercase a-z only",
      "Whitespace and trailing/leading spaces in parsing",
      "Pattern longer than text in substring search",
    ],
    commonMistakes: [
      "Using += to build a string in a loop, making it O(n^2)",
      "Comparing sorted strings (O(n log n)) when a Counter (O(n)) suffices",
      "Forgetting to normalize case or strip punctuation before comparing",
      "Assuming ASCII when input may contain Unicode characters",
      "Off-by-one when two pointers cross in a palindrome check",
      "Rebuilding the frequency map from scratch each window instead of updating it",
    ],
    template: {
      language: "python",
      code: "from collections import Counter\n\ndef is_palindrome(s):\n    # ignore case and non-alphanumeric characters\n    i, j = 0, len(s) - 1\n    while i < j:\n        while i < j and not s[i].isalnum():\n            i += 1\n        while i < j and not s[j].isalnum():\n            j -= 1\n        if s[i].lower() != s[j].lower():\n            return False\n        i += 1\n        j -= 1\n    return True\n\ndef is_anagram(a, b):\n    return Counter(a) == Counter(b)\n\ndef reverse_words(s):\n    return ' '.join(reversed(s.split()))",
    },
    observations: [
      "Two strings are anagrams iff their Counters (or 26-slot arrays) are equal",
      "Build strings with a list + ''.join to stay O(n); += in a loop is O(n^2)",
      "For lowercase-only input, a length-26 list out-performs a dict and compares directly",
      "Reversing 'words' is just split, reverse the token list, then join",
      "KMP's prefix function reuses partial matches so the text pointer never moves backward",
    ],
    problems: [
      { title: "Valid Anagram", url: "https://leetcode.com/problems/valid-anagram/", difficulty: "Easy", pattern: "frequency-count equality" },
      { title: "Valid Palindrome", url: "https://leetcode.com/problems/valid-palindrome/", difficulty: "Easy", pattern: "two pointers skipping non-alphanumerics" },
      { title: "Group Anagrams", url: "https://leetcode.com/problems/group-anagrams/", difficulty: "Medium", pattern: "hash map keyed by sorted/count signature" },
      { title: "Longest Palindromic Substring", url: "https://leetcode.com/problems/longest-palindromic-substring/", difficulty: "Medium", pattern: "expand around each center" },
      { title: "Find the Index of the First Occurrence in a String", url: "https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/", difficulty: "Easy", pattern: "substring search / KMP" },
    ],
    videos: [
      { title: "NeetCode 150 Ep.1: Arrays & Hashing (covers string frequency problems)", url: "https://www.youtube.com/watch?v=IiDuXLqV6e4", source: "NeetCode" },
      { title: "Algorithms in Python - Full Course for Beginners", url: "https://www.youtube.com/watch?v=fW_OS3LGB9Q", source: "freeCodeCamp" },
      { title: "Arrays & Hashing (NeetCode 150 playlist)", url: "https://www.youtube.com/playlist?list=PLALUz6Z8Un2ew_yN3UAce8bOA25P5kaUl", source: "NeetCode" },
    ],
    articles: [
      { title: "Strings in Python", url: "https://www.geeksforgeeks.org/python-strings/", source: "GeeksforGeeks" },
      { title: "Prefix function — Knuth-Morris-Pratt", url: "https://cp-algorithms.com/string/prefix-function.html", source: "CP-Algorithms" },
      { title: "String (computer science)", url: "https://en.wikipedia.org/wiki/String_(computer_science)", source: "Wikipedia" },
    ],
  },

  "hashing": {
    id: "hashing",
    name: "Hash Maps / Hash Sets",
    summary: "Average O(1) lookup, insert, and membership — the go-to when you need to remember what you've seen.",
    overview:
      "## The core idea\n\nA hash map stores key->value pairs and a hash set stores keys, both giving **average `O(1)`** insert, delete, and lookup. A hash function turns a key into a bucket index; collisions are resolved by chaining or open addressing. This trades memory for speed: you spend `O(n)` space to turn repeated `O(n)` scans into single `O(1)` probes.\n\n## How to think\n\nThe trigger phrase is **\"have I seen this before?\"** or **\"how many times?\"**. Whenever a brute-force solution does a nested loop to find a matching/duplicate/complement element, a hash structure usually collapses it to one pass. Ask: what do I need to remember as I scan — a **set** of seen values, a **count** per value, or a **mapping** from value to index/group? In Python that's `set`, `collections.Counter`, and `dict`. The classic move is the **complement trick**: for Two Sum, store each number and check if `target - x` already exists.\n\n## Common patterns\n\n- **Seen-set** — detect duplicates / cycles / first repeat.\n- **Frequency map** — `Counter` for counts, majority, top-k.\n- **Complement / index map** — Two Sum, pair-with-difference.\n- **Group-by signature** — bucket anagrams or items sharing a computed key.\n\n## Edge thinking\n\nKeys must be **hashable** — use tuples, not lists. `O(1)` is *average*; worst case is `O(n)` under adversarial collisions, so never claim it unconditionally. Watch insertion-order assumptions (Python dicts preserve insertion order, but don't rely on it for correctness) and remember a set discards duplicates and ordering.",
    recognition: [
      "You catch yourself writing a nested loop just to find a matching element",
      "The question asks 'does X exist?', 'how many of X?', or 'first repeated X'",
      "You need the complement (target - x) or a pair with a fixed difference",
      "Grouping items that share a computed signature (anagrams, equal rows)",
      "Deduplicating or counting distinct elements",
      "Caching/memoizing results keyed by a state you'll revisit",
    ],
    whenToUse: [
      "Membership tests or duplicate detection in O(1) average",
      "Counting frequencies (Counter) for majority / top-k / mode",
      "Mapping a value to its index, group, or last-seen position",
      "Replacing an O(n^2) pair search with a single O(n) pass",
    ],
    whenNotToUse: [
      "You need sorted order or range queries — use a tree / sorted structure",
      "Keys are unhashable (mutable lists) without converting to tuples",
      "Memory is the binding constraint and an in-place O(1)-space trick exists",
      "Worst-case O(1) is required (real-time) — hashing only guarantees average",
    ],
    patterns: [
      { name: "Seen-set", note: "add as you scan; membership check detects duplicates/cycles" },
      { name: "Frequency map", note: "Counter(values); supports counts, majority, top-k" },
      { name: "Complement / index map", note: "store value->index; check target - x exists (Two Sum)" },
      { name: "Group-by signature", note: "dict of computed-key -> list of items (anagrams)" },
    ],
    complexity: [
      { operation: "Insert / lookup / delete (average)", time: "O(1)", space: "O(n)", note: "amortized over rehashes" },
      { operation: "Insert / lookup (worst case)", time: "O(n)", space: "O(n)", note: "adversarial collisions" },
      { operation: "Build Counter from n items", time: "O(n)", space: "O(k)", note: "k = distinct keys" },
      { operation: "Iterate all entries", time: "O(n)", space: "O(1)", note: "no order guarantee for correctness" },
      { operation: "Membership test x in set", time: "O(1) average", space: "O(1)", note: "vs O(n) list scan" },
    ],
    edgeCases: [
      "Empty input — Counter/dict are empty, guard before .most_common()",
      "Duplicate keys overwrite values; decide first-wins vs last-wins",
      "Unhashable keys (lists) raise TypeError — convert to tuple/frozenset",
      "Negative numbers and zero are valid keys; don't special-case them away",
      "Counting needs default handling — use Counter or dict.get(k, 0)",
      "Hash collisions degrade to O(n); state 'average' when quoting O(1)",
    ],
    commonMistakes: [
      "Quoting O(1) without noting it is average, not worst-case",
      "Using a list as a dict key (unhashable) instead of a tuple",
      "Checking the map for the complement after inserting the current element (Two Sum self-match)",
      "Mutating a dict/set while iterating over it",
      "Relying on dict ordering for correctness rather than using it as an optimization",
      "Rebuilding a frequency map from scratch when an incremental update would do",
      "Using dict[k] += 1 without initializing — use Counter or defaultdict(int)",
    ],
    template: {
      language: "python",
      code: "from collections import Counter, defaultdict\n\ndef two_sum(nums, target):\n    seen = {}                       # value -> index\n    for i, x in enumerate(nums):\n        if target - x in seen:      # check BEFORE inserting x\n            return [seen[target - x], i]\n        seen[x] = i\n    return []\n\ndef group_anagrams(words):\n    groups = defaultdict(list)\n    for w in words:\n        key = tuple(sorted(w))      # hashable signature\n        groups[key].append(w)\n    return list(groups.values())\n\ndef has_duplicate(nums):\n    seen = set()\n    for x in nums:\n        if x in seen:\n            return True\n        seen.add(x)\n    return False",
    },
    observations: [
      "The complement check must happen before inserting the current element, or x matches itself",
      "Counter(a) == Counter(b) is the cleanest anagram test, in O(n)",
      "A frozenset or sorted tuple is the canonical hashable group-by key",
      "set membership is O(1) average vs O(n) for list 'in' — a frequent speedup",
      "Trading O(n) memory for O(1) lookup is the central hashing bargain; weigh it explicitly",
    ],
    problems: [
      { title: "Contains Duplicate", url: "https://leetcode.com/problems/contains-duplicate/", difficulty: "Easy", pattern: "seen-set membership" },
      { title: "Two Sum", url: "https://leetcode.com/problems/two-sum/", difficulty: "Easy", pattern: "complement index map" },
      { title: "Group Anagrams", url: "https://leetcode.com/problems/group-anagrams/", difficulty: "Medium", pattern: "group-by sorted/count signature" },
      { title: "Top K Frequent Elements", url: "https://leetcode.com/problems/top-k-frequent-elements/", difficulty: "Medium", pattern: "frequency map + bucket/heap" },
      { title: "Longest Consecutive Sequence", url: "https://leetcode.com/problems/longest-consecutive-sequence/", difficulty: "Medium", pattern: "set lookups to extend runs in O(n)" },
    ],
    videos: [
      { title: "NeetCode 150 Ep.1: Arrays & Hashing Explained", url: "https://www.youtube.com/watch?v=IiDuXLqV6e4", source: "NeetCode" },
      { title: "Hash Table Hash Function", url: "https://www.youtube.com/watch?v=2E54GqF0H4s", source: "WilliamFiset" },
      { title: "Data Structures playlist (Hash Tables section)", url: "https://www.youtube.com/playlist?list=PLDV1Zeh2NRsB6SWUrDFW2RmDotAfPbeHu", source: "WilliamFiset" },
    ],
    articles: [
      { title: "Hashing Data Structure", url: "https://www.geeksforgeeks.org/hashing-data-structure/", source: "GeeksforGeeks" },
      { title: "Hash table", url: "https://en.wikipedia.org/wiki/Hash_table", source: "Wikipedia" },
      { title: "Basics of Hash Tables", url: "https://www.hackerearth.com/practice/data-structures/hash-tables/basics-of-hash-tables/tutorial/", source: "HackerEarth" },
    ],
  },

  "two-pointers": {
    id: "two-pointers",
    name: "Two Pointers",
    summary: "Two indices walking an array or string to replace a nested loop with a single linear pass.",
    overview:
      "## The core idea\n\nTwo pointers keeps **two indices** moving over a sequence so that work that looks `O(n^2)` becomes `O(n)`. The two common shapes are **opposite ends** (`left` at the start, `right` at the end, moving toward each other) and **same direction** (a slow `write`/`read` pair, or fast/slow for cycle detection). The pointers encode an invariant so you never re-examine elements.\n\n## How to think\n\nAsk: **is the data sorted, or can I sort it?** If yes and you're hunting a pair/triple meeting a numeric condition, opposite-end pointers shrink the search by deciding at each step which end to move (sum too small -> move `left` right; too big -> move `right` left). If the task is **in-place filtering or partitioning**, use a same-direction slow/fast pair where slow marks the next write position. If it's **cycle or midpoint** detection in a linked list, use fast/slow (Floyd's). The key is that each move must **provably eliminate** options so you can justify the linear bound.\n\n## Common patterns\n\n- **Converging (opposite ends)** — sorted pair/triple sums, container-with-most-water.\n- **Same-direction (slow/fast write)** — remove duplicates/zeros in place.\n- **Fast & slow (Floyd)** — cycle detection, find middle, k-th from end.\n- **Two sequences** — merge two sorted arrays/lists.\n\n## Edge thinking\n\nThe loop guard is almost always `while left < right` (strictly less) for converging pointers — getting `<=` wrong causes a self-pair. After sorting for triples, **skip duplicates** to avoid repeated answers. And remember: opposite-end two pointers is only valid when the array is **sorted or monotonic** in the quantity you're comparing.",
    recognition: [
      "The array is sorted (or you may sort it) and you seek a pair/triple with a target sum",
      "A brute-force nested loop compares elements from two positions",
      "In-place removal/partition where order of kept elements is preserved",
      "Palindrome or symmetry check comparing ends inward",
      "Merging two already-sorted sequences",
      "Linked-list cycle, midpoint, or k-th-from-end detection",
    ],
    whenToUse: [
      "Finding pairs/triples summing to a target in a sorted array",
      "In-place compaction (remove duplicates / move zeroes) with O(1) space",
      "Comparing or merging two sorted sequences in linear time",
      "Maximizing an area/width where moving the limiting pointer can only help",
    ],
    whenNotToUse: [
      "Data is unsorted and sorting would destroy required original indices",
      "The condition isn't monotonic, so narrowing one side isn't provably safe",
      "You need every subarray/window — that's sliding window, not a pair search",
      "Random access by value is needed — a hash map fits better",
    ],
    patterns: [
      { name: "Converging", note: "left/right from the ends move inward on a sorted array" },
      { name: "Same-direction (slow/fast)", note: "slow = next write index for in-place filtering" },
      { name: "Fast & slow (Floyd)", note: "two-speed pointers for cycle / midpoint detection" },
      { name: "Two sequences", note: "one pointer per sorted list to merge or intersect" },
    ],
    complexity: [
      { operation: "Single converging pass", time: "O(n)", space: "O(1)", note: "after any sorting cost" },
      { operation: "Sort then two-pointer (e.g. 3Sum)", time: "O(n^2)", space: "O(1)", note: "outer fix + inner scan" },
      { operation: "In-place compaction", time: "O(n)", space: "O(1)", note: "slow/fast write pointer" },
      { operation: "Merge two sorted arrays", time: "O(n + m)", space: "O(n + m)", note: "or O(1) if merged in place from the back" },
      { operation: "Floyd cycle detection", time: "O(n)", space: "O(1)", note: "no extra visited set" },
    ],
    edgeCases: [
      "Empty or single-element input — loop body may never run",
      "left < right vs left <= right (avoid pairing an element with itself)",
      "Duplicate values producing repeated triples — skip equal neighbors",
      "All elements equal, or already sorted/reverse-sorted",
      "Two pointers crossing exactly in the middle (even vs odd length)",
      "Negative numbers shifting where the target sum sits",
    ],
    commonMistakes: [
      "Using opposite-end pointers on unsorted data",
      "Forgetting to skip duplicates after sorting, yielding repeated answers",
      "Wrong loop guard (<= instead of <) causing a self-pair or out-of-bounds",
      "Moving the wrong pointer when the sum is off (move left up if sum too small)",
      "Advancing both pointers every iteration when only one should move",
      "Sorting when the problem needs original indices (then losing them)",
    ],
    template: {
      language: "python",
      code: "def two_sum_sorted(nums, target):\n    # nums is sorted ascending\n    left, right = 0, len(nums) - 1\n    while left < right:\n        s = nums[left] + nums[right]\n        if s == target:\n            return [left, right]\n        elif s < target:\n            left += 1          # need a bigger sum\n        else:\n            right -= 1         # need a smaller sum\n    return []\n\ndef remove_duplicates(nums):\n    # in-place; returns new length, nums sorted\n    if not nums:\n        return 0\n    slow = 0\n    for fast in range(1, len(nums)):\n        if nums[fast] != nums[slow]:\n            slow += 1\n            nums[slow] = nums[fast]\n    return slow + 1",
    },
    observations: [
      "Each iteration must eliminate at least one candidate, which is why it's O(n) not O(n^2)",
      "For 3Sum, fix one element then two-pointer the rest: O(n^2) overall, O(1) extra space",
      "Merging from the back avoids overwriting unprocessed elements (in-place merge)",
      "Floyd's tortoise-and-hare finds a cycle with no extra memory vs an O(n) visited set",
      "Container-with-water moves the shorter wall because the taller one can never improve by moving",
    ],
    problems: [
      { title: "Valid Palindrome", url: "https://leetcode.com/problems/valid-palindrome/", difficulty: "Easy", pattern: "converging ends, skip non-alphanumerics" },
      { title: "Two Sum II - Input Array Is Sorted", url: "https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/", difficulty: "Medium", pattern: "converging pointers on sorted array" },
      { title: "3Sum", url: "https://leetcode.com/problems/3sum/", difficulty: "Medium", pattern: "fix one + two-pointer, skip duplicates" },
      { title: "Container With Most Water", url: "https://leetcode.com/problems/container-with-most-water/", difficulty: "Medium", pattern: "converging ends, move the shorter wall" },
      { title: "Trapping Rain Water", url: "https://leetcode.com/problems/trapping-rain-water/", difficulty: "Hard", pattern: "two pointers with running left/right max" },
    ],
    videos: [
      { title: "L1. Introduction to Sliding Window and 2 Pointers | Templates | Patterns", url: "https://www.youtube.com/watch?v=9kdHxplyl5I", source: "takeUforward" },
      { title: "Two Pointer and Sliding Window (full playlist)", url: "https://www.youtube.com/playlist?list=PLgUwDviBIf0q7vrFA_HEWcqRqMpCXzYAL", source: "takeUforward" },
      { title: "NeetCode 150 Ep.1: Arrays & Hashing (two-pointer problems)", url: "https://www.youtube.com/watch?v=IiDuXLqV6e4", source: "NeetCode" },
    ],
    articles: [
      { title: "Two Pointers Technique", url: "https://www.geeksforgeeks.org/two-pointers-technique/", source: "GeeksforGeeks" },
      { title: "Two Pointers", url: "https://usaco.guide/silver/two-pointers", source: "USACO Guide" },
      { title: "Two Pointer (blogs)", url: "https://takeuforward.org/blogs/two-pointers", source: "takeUforward" },
    ],
  },

  "sliding-window": {
    id: "sliding-window",
    name: "Sliding Window",
    summary: "Maintain a contiguous range that expands and contracts, turning re-scans into one O(n) pass.",
    overview:
      "## The core idea\n\nA sliding window keeps a **contiguous range** `[left, right]` over an array or string and reuses work as it moves, so problems that look like \"check every subarray\" (`O(n^2)`) collapse to a single `O(n)` pass. As `right` advances you **add** the entering element; when the window becomes invalid (or you've passed the fixed length) you **shrink** from `left`, subtracting the leaving element.\n\n## How to think\n\nAsk three questions. **What makes a window valid?** (sum <= S, at most k distinct, no repeated char). **How does state update when `right` moves in?** (increment a count, add to a sum). **When and how do I shrink `left`?** If the window has a **fixed length k**, slide by one each step. If it's **variable**, grow greedily and shrink only while the constraint is violated — and the choice of `if` vs `while` for shrinking is the crux: variable windows almost always need a **`while`** loop. Track the answer (max length, min length, count) at the right moment relative to the shrink.\n\n## Common patterns\n\n- **Fixed window** — length-k max/avg/sum; add entering, drop leaving.\n- **Variable window (longest)** — grow, shrink while invalid, record max.\n- **Variable window (shortest)** — grow until valid, shrink while still valid, record min.\n- **Window + frequency map** — at-most-k-distinct, anagram/substring matching.\n\n## Edge thinking\n\nGuard the empty input and the case where the window is larger than the array. For 'shortest valid' you record the answer **inside** the shrink loop; for 'longest valid' you record **after** shrinking restores validity. Forgetting to subtract the leaving element from the frequency map is the single most common bug.",
    recognition: [
      "Asks for the longest/shortest/optimal contiguous subarray or substring",
      "A constraint on the window: sum, count of distinct, no-repeat, at-most-k",
      "Brute force would re-scan overlapping subarrays (O(n^2) or worse)",
      "Fixed-length-k aggregate (max average, max sum of k elements)",
      "Substring containing/matching a target multiset of characters",
      "Counting subarrays whose property holds (often via at-most minus at-most)",
    ],
    whenToUse: [
      "Optimizing over contiguous subarrays/substrings with a monotonic validity condition",
      "Fixed-size window aggregates computed incrementally",
      "Longest/shortest substring under a frequency or distinctness constraint",
      "Replacing nested-loop subarray scanning with one linear pass",
    ],
    whenNotToUse: [
      "The subset need not be contiguous (then it's subsequence DP, not a window)",
      "The validity condition isn't monotonic, so shrinking doesn't restore validity",
      "Negative numbers break the 'shrink when sum too big' monotonicity (use prefix sums + map)",
      "You need all pairs regardless of adjacency — that's two pointers or hashing",
    ],
    patterns: [
      { name: "Fixed window", note: "slide a length-k window; add entering, drop leaving element" },
      { name: "Variable: longest", note: "expand right, shrink left while invalid, track max length" },
      { name: "Variable: shortest", note: "expand until valid, shrink while still valid, track min length" },
      { name: "Window + counts", note: "frequency map for at-most-k-distinct or substring matching" },
    ],
    complexity: [
      { operation: "Single pass (each element in/out once)", time: "O(n)", space: "O(k)", note: "k = window contents / distinct keys" },
      { operation: "Fixed window of size k", time: "O(n)", space: "O(1)", note: "running sum, no map needed" },
      { operation: "Variable window + frequency map", time: "O(n)", space: "O(min(n, alphabet))", note: "map of window counts" },
      { operation: "Window maximum (monotonic deque)", time: "O(n)", space: "O(k)", note: "deque of candidate indices" },
      { operation: "Count subarrays via atMost(k) - atMost(k-1)", time: "O(n)", space: "O(k)", note: "two window passes" },
    ],
    edgeCases: [
      "Empty input or window larger than the array",
      "k = 0 or k = n (degenerate fixed windows)",
      "All identical characters / all distinct characters",
      "Target requires characters absent from the string (no valid window)",
      "Single-element array with a fixed window of size 1",
      "Negative numbers invalidating the shrink-on-overflow assumption",
    ],
    commonMistakes: [
      "Using if instead of while to shrink a variable window",
      "Forgetting to subtract the leaving element from the running sum / frequency map",
      "Recording the answer at the wrong moment (before vs after shrinking)",
      "Letting a count in the map go to zero but not deleting the key (breaks 'distinct' counts)",
      "Re-scanning the whole window each step instead of incremental updates (back to O(n*k))",
      "Off-by-one in window length: right - left + 1, not right - left",
      "Applying the technique to non-contiguous selections",
    ],
    template: {
      language: "python",
      code: "from collections import defaultdict\n\ndef longest_no_repeat(s):\n    # longest substring with all distinct characters (variable window)\n    count = defaultdict(int)\n    left = best = 0\n    for right, ch in enumerate(s):\n        count[ch] += 1\n        while count[ch] > 1:          # shrink WHILE invalid\n            count[s[left]] -= 1\n            left += 1\n        best = max(best, right - left + 1)\n    return best\n\ndef max_sum_fixed(nums, k):\n    # max sum of any length-k window (fixed window)\n    if k > len(nums):\n        return 0\n    window = sum(nums[:k])\n    best = window\n    for right in range(k, len(nums)):\n        window += nums[right] - nums[right - k]\n        best = max(best, window)\n    return best",
    },
    observations: [
      "Each element enters and leaves the window at most once, which is why the pass is O(n)",
      "Variable windows need 'while invalid: shrink'; an if only removes one element",
      "Window length is right - left + 1 — a frequent off-by-one",
      "Count exactly-k subarrays as atMost(k) - atMost(k-1) when direct counting is awkward",
      "A monotonic deque turns sliding-window maximum into O(n) instead of O(n*k)",
      "Subtracting the leaving element is what makes the work incremental — never skip it",
    ],
    problems: [
      { title: "Maximum Average Subarray I", url: "https://leetcode.com/problems/maximum-average-subarray-i/", difficulty: "Easy", pattern: "fixed window of size k" },
      { title: "Longest Substring Without Repeating Characters", url: "https://leetcode.com/problems/longest-substring-without-repeating-characters/", difficulty: "Medium", pattern: "variable window + last-seen / count map" },
      { title: "Longest Repeating Character Replacement", url: "https://leetcode.com/problems/longest-repeating-character-replacement/", difficulty: "Medium", pattern: "window valid while len - maxFreq <= k" },
      { title: "Permutation in String", url: "https://leetcode.com/problems/permutation-in-string/", difficulty: "Medium", pattern: "fixed window + frequency match" },
      { title: "Minimum Window Substring", url: "https://leetcode.com/problems/minimum-window-substring/", difficulty: "Hard", pattern: "shortest window covering a target multiset" },
    ],
    videos: [
      { title: "L1. Introduction to Sliding Window and 2 Pointers | Templates | Patterns", url: "https://www.youtube.com/watch?v=9kdHxplyl5I", source: "takeUforward" },
      { title: "L12. Minimum Window Substring", url: "https://www.youtube.com/watch?v=WJaij9ffOIY", source: "takeUforward" },
      { title: "Two Pointer and Sliding Window (full playlist)", url: "https://www.youtube.com/playlist?list=PLgUwDviBIf0q7vrFA_HEWcqRqMpCXzYAL", source: "takeUforward" },
    ],
    articles: [
      { title: "Window Sliding Technique", url: "https://www.geeksforgeeks.org/window-sliding-technique/", source: "GeeksforGeeks" },
      { title: "Sliding Window", url: "https://usaco.guide/gold/sliding-window", source: "USACO Guide" },
      { title: "Sliding Window Technique - Tutorial", url: "https://takeuforward.org/data-structure/sliding-window-technique", source: "takeUforward" },
    ],
  },
};
